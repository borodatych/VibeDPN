// ─────────────────────────────────────────────────────────────────────────────
//  .vibe/agents.json — внешние агенты проекта (проектный формат набора)
//
//  Это РАБОЧИЙ файл, а не пример: файла-двойника рядом больше нет. Комментарии писать
//  МОЖНО — формат JSONC, как и у остальных конфигов .vibe.
//  Все записи выключены ("active": false) — снимите поле у той, которой будете пользоваться.
//  ВНИМАНИЕ, ОБЛАСТИ: этот файл описывает агентов ПРОЕКТА и едет в репозитории. Машинный
//  реестр агентов — деталь конкретного продукта: он есть не у всех, путь у каждого свой,
//  и набор его намеренно не называет — смотрите документацию своего продукта. Там, где он
//  есть, проектная запись сильнее машинной при совпадении имени.
//
//  Зачем: описывает, кого можно позвать в эту рабочую папку кроме собственного
//  агента самой IDE. Внешний агент запускается отдельным процессом и говорит с IDE
//  на ACP (Agent Client Protocol) — спрашивает разрешение перед правкой, а IDE
//  снимает по его правкам чекпоинт и пишет их в журнал.
//  Нет файла — внешних агентов нет; всё остальное работает как раньше.
//
//  Не хотите писать руками — попросите агента в чате:
//    «Создай мне .vibe/agents.json для этого проекта»
//  (полная спека формата — docs/manuals/agentsSpec.md, её можно скормить любой LLM).
//
//  Главные принципы:
//   • "id" + "command" — единственные обязательные поля.
//   • Команда запускается НАПРЯМУЮ, без оболочки: аргументы перечисляются
//     по одному в "args", а не пишутся строкой в "command".
//   • Агент должен уметь ACP. Обычный CLI сюда не годится: у большинства
//     инструментов режим ACP даёт отдельный адаптер (см. пример ниже).
//   • Одна битая запись не роняет реестр — пропускается с предупреждением.
//   • "active": false оставляет запись документированной, но вне списка.
// ─────────────────────────────────────────────────────────────────────────────
{
	"version": 1,
	"agents": [
		// ─── Claude Code через официальный ACP-адаптер ──────────────────────
		// Требует входа: выполните `claude /login` в терминале один раз.
		// Сам `claude` разговаривать по ACP не умеет — адаптер обязателен.
		//
		// ВНИМАНИЕ, ЧЕМ ВЫ ПЛАТИТЕ: адаптер берёт ANTHROPIC_API_KEY из окружения, а если его
		// нет — подхватывает вход Claude Code. То есть ключ в окружении МОЛЧА побеждает
		// подписку, и вместо лимитов плана вы платите за токены (code.claude.com/docs/en/costs,
		// проверено 09.09.2026). Держите ключ Anthropic в настройках IDE, а не в окружении, либо
		// снимайте его для этой записи: "env": { "ANTHROPIC_API_KEY": "" }.
		//
		// Версия НЕ ПРИПИНОВАНА намеренно — «свежий адаптер» удобнее в чате. Нужна
		// воспроизводимость (CI, общая на команду настройка) — допишите её прямо в имя пакета:
		// "args": ["-y", "@agentclientprotocol/claude-agent-acp@0.75.1"]. Официальный реестр
		// протокола пинует версию именно так, и кнопка «Каталог ACP-агентов» это наследует.
		{
			"id": "claude", "active": false,
			"name": "Claude Code",
			"command": "npx",
			"args": ["-y", "@agentclientprotocol/claude-agent-acp"]
		},

		// ─── OpenHands (нативный режим ACP) ─────────────────────────────────
		// Адаптер не нужен: CLI умеет ACP сам, подкоманда — `acp`, транспорт JSON-RPC по stdio
		// (docs.openhands.dev/openhands/usage/cli/ide/overview, проверено 09.09.2026).
		// Лицензия MIT.
		//
		// Установка одним из двух способов, интерпретатор нужен свой:
		//   uv tool install openhands --python 3.12     (нужен Python 3.12+)
		//   curl -fsSL https://install.openhands.dev/install.sh | sh
		// На Windows работает ТОЛЬКО через WSL.
		//
		// В официальном реестре ACP его нет — поэтому запись здесь, а не в каталоге агентов.
		//
		// Своя песочница: по умолчанию агент исполняется локально и переключается в Docker,
		// когда нужна изоляция. Полезные переменные окружения, если Docker включён:
		//   SANDBOX_VOLUMES=$PWD:/workspace:rw   — что именно смонтировать (одну папку проекта);
		//   SANDBOX_NETWORK_DISABLED=true        — отрезать агенту сеть.
		// Подтверждения у него свои, три режима: Always Ask (по умолчанию), Always Approve и
		// LLM-Based; переключаются на лету командой `/confirm always-ask`. Наши diff-превью,
		// чекпоинты и журнал при этом работают как обычно — они на стороне IDE.
		{
			"id": "openhands",
			"name": "OpenHands",
			"command": "openhands",
			"args": ["acp"],
			"active": false
		},

		// ─── Cursor Agent (нативный режим ACP) ──────────────────────────────
		// Адаптер не нужен: CLI Cursor умеет ACP сам. Бинарь называется `agent`
		// (он ставится вместе с приложением Cursor, обычно ~/.local/bin/agent),
		// подкоманда — `acp`. Ключ передаётся флагом ДО подкоманды, поэтому в
		// "args" он идёт первым: ["--api-key", "…", "acp"].
		// Режимы agent / plan / ask — это штатные режимы сессии ACP: переключаются
		// пикером над композером, plan и ask доступны только на чтение.
		// MCP-серверы берутся из .cursor/mcp.json (проектный и пользовательский);
		// серверы уровня команды из дашборда Cursor в режиме ACP НЕ подхватываются.
		// Вход: `cursor login` один раз в терминале. Ключ в файл репозитория не
		// пишите — либо вход, либо CURSOR_API_KEY в окружении машины и запись вида
		// "args": ["--api-key", "$CURSOR_API_KEY", "acp"] в СВОЁМ agents.json.
		// Источник: cursor.com/docs/cli/acp (проверено 08.09.2026).
		{
			"id": "cursor",
			"name": "Cursor Agent",
			"command": "agent",
			"args": ["acp"],
			"active": false
		},

		// ─── Агент для одного пакета монорепо ───────────────────────────────
		// "dir" задаёт рабочую папку относительно корня проекта: агент увидит
		// её как свой корень и не полезет в соседние пакеты.
		{
			"id": "web",
			"name": "Claude Code (web)",
			"command": "npx",
			"args": ["-y", "@agentclientprotocol/claude-agent-acp"],
			"dir": "packages/web",
			"active": false
		},

		// ─── Свой агент с переменными окружения ─────────────────────────────
		// "env" кладётся поверх унаследованного окружения. Ключи в файл
		// репозитория не пишите — передавайте имя переменной, а значение
		// держите в окружении машины.
		{
			"id": "custom",
			"name": "Собственный ACP-агент",
			"command": "/usr/local/bin/my-acp-agent",
			"args": ["--stdio"],
			"env": { "MY_AGENT_PROFILE": "project" },
			"active": false
		}
	]
}
