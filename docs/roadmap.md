# Roadmap VibeDPN

Один чекбокс — одна итерация; стадия закрыта, когда прошли её проверки (`CLAUDE.md`).
Выполнено — отмечается `[x]` с датой и веткой. Концепт и архитектура — [idea.md](idea.md);
грабли и проверенные факты — [knowledge/](knowledge/README.md).

## Stage 0 — Каркас

- [x] **Структура репо, `.vibe/` со стандартным набором + `vibedpn.mdc`** — ✅ (2026-09-11, `next`) дерево по §5 idea.md
      (пути доков — по соглашениям проекта, см. [README.md](README.md)), `.vibe/` из канона
      VibeBrains, правила §11 в `.vibe/rules/vibedpn.mdc` и `CLAUDE.md`
- [x] **`compose.yaml` со всеми сервисами и профилями** — ✅ (2026-09-11, `next`) семь сервисов, сеть `vibedpn-upstreams` (`nat-unprotected`, статические IP шлюзов), образы `core`/`wg`/`ui` собираются и проходят smoke (core `/health` через nginx-прокси ui); entrypoint `wg` — контракт режимов, реализация в Stage 3; живая техспека `docs/techSpec.md`, мануал среды `docs/manuals/devSetup.md`
- [x] **`config.example.yaml` + Pydantic-модель `config.py`** — ✅ (2026-09-11, `next`) валидация ролей и профилей,
      спека формата [manuals/configSpec.md](manuals/configSpec.md)
- [x] **CI** — ✅ (2026-09-11, `next`) lint (ruff, mypy strict, eslint через `npm run lint` когда появится `ui/package.json`), pytest, shellcheck, actionlint, `compose config`, buildx multi-arch (amd64+arm64) сборка `core`, `wg`, `ui` в GHCR; первый прогон в GitHub 2026-09-12 (run 34690322430) зелёный целиком, включая multi-arch сборку и публикацию в GHCR
- [x] **README** — ✅ (2026-09-11, `next`) три сценария в одном абзаце каждый, статус и предупреждение об exit-ноде (заполняется по мере стадий)
- [x] **Репозиторий на GitHub** — ✅ (2026-09-12, `next`) публичный `github.com/borodatych/VibeDPN`, образы `ghcr.io/borodatych/vibedpn-{core,wg,ui}`; VibeBrainsProjects из kickoff не используется — решение владельца
- [x] **Ревью Stage 0** — ✅ (2026-09-11, `next`) состязательное ревью шестью линзами: главная находка — `trusted_host_interfaces` не пропускает транзит в бридж, сеть переведена на `nat-unprotected` с обязательным nft-drop прямого доступа в Stage 4; `peer_config` убран из конфига (фиксированный `secrets/wg-client.conf`), bind-mount файлов без создания каталогов, чистые ошибки конфига у `vibedpn-core`, IPv4-валидация `endpoint`, проверка `lan_address` без перебора /8, пин ruff, `concurrency` и пиновый actionlint в CI, `latest` строго для `main`

## Stage 1 — CLI и install

- [x] **`install.sh`** — ✅ (2026-09-12, `next`) детект ОС/арх (Debian 12/13, Raspberry Pi OS 64-bit, amd64/arm64), Docker Engine + Compose из официального apt-репо (deb822), clone в `/opt/vibedpn`, venv на системном Python (floor опущен до 3.11 ради bookworm), зависимости по `core/requirements.txt` с sha256, симлинк, группа `docker`; идемпотентен; тест в CI дважды в чистых bookworm/trixie-контейнерах, матрица Python 3.11/3.12/3.13; мануал [manuals/installation.md](manuals/installation.md)
- [x] **`vibedpn init`** — ✅ (2026-09-12, `next`) детект интерфейса с default route, адреса и подсети
      (`ip -j`, парсеры на реальных фикстурах), публичного адреса для vps и модуля `wireguard`;
      вопросы только роль, пароль UI и peer-файл, всё задаваемо флагами; `config.yaml` рендерится из
      шаблона с пояснениями (образец `config.example.yaml` — его рендер, тест держит их равными),
      `.env` из `Config.env_vars()`, `secrets/htpasswd` (bcrypt, проверен с nginx `auth_basic`),
      `secrets/wg-client.conf` 600; повторный запуск — отказ без `--force`, бэкап с ним; `init`
      прогоняется в CI-тесте установки. Ревью (3 линзы + скептик): лимит bcrypt 72 байта с понятной
      ошибкой, все отказы через `error:` без трассировок, сборка содержимого до первой записи,
      ранние отказы до вопросов, флаги чужой роли, `~` в промпте, секреты прежней роли в `.bak`,
      честная негативная проверка в CI
- [x] **`vibedpn up|down|restart|status|logs`** — ✅ (2026-09-12, `next`) поверх `docker compose --project-directory`; `up -d --remove-orphans` с пересборкой `.env` из `config.yaml`, `down` со всеми профилями, `restart` = up + restart (bind-mount конфига пересоздание не вызывает), `status` из конфига и `ps --format json`, `logs` с `-f`/`--tail`; preflight переводит ошибки Docker (нет CLI, нет демона, нет прав на сокет) в одну строку; smoke на colima: up только `core` → healthy → logs → restart → down. Ревью (2 линзы + скептик): `init` под sudo отдаёт `config.yaml`/`.env` пользователю (иначе `up` без sudo падал), `up`/`restart` гасят контейнеры выпавших профилей (`--remove-orphans` их не трогает — проверено), тег образов из ветки чекаута (`next` → `:next`, а не несуществующий `latest`)
- [x] **`vibedpn doctor` v1** — ✅ (2026-09-12, `next`) таблица проверок с вердиктом, деталью и подсказкой: config/.env/secrets, модули `wireguard` (fail только при настроенном туннеле) и `nf_tables`, `ip_forward`, порты роли по `ss -H -lntup` с учётом своих контейнеров и заглушки resolved, Docker и сервисы; `--json`; exit 1 при fail; парсеры на реальных фикстурах, CI-тест установки гоняет `doctor`; только проверяет, не чинит (§11.2). Ревью (2 линзы + скептик): неснятый факт — `warn`, не ложный вердикт (root-only `secrets/`, `modprobe` вне PATH, отсутствие `ss`, недоступный Docker при занятых портах), упавший контейнер — `fail`, подсказка при битом конфиге ведёт к правке, а не к `init --force`, порты ноды в проверке, модуль WireGuard обязателен только для профилей wg-*

## Stage 2 — Роль vps: нода

- [x] **`myst-provider` контейнер** — ✅ (2026-09-12, `next`) сервис с published-портами (loopback 4449/4050, UDP-диапазон), томом `data/myst-provider`, актуальными флагами; пароль панели ноды — bcrypt-файл `nodeui-pass` от `init` (нода читает его при каждом входе, проверено на 1.39.5), пароль спрашивается для всех ролей; TequilAPI, как выяснилось, без аутентификации вовсе — защита сетевая (loopback, nft в Stage 4/8); регистрация и клейм — чекбокс 4 на реальной VPS. Ревью (2 линзы + скептик): порог Docker Engine ≥ 28.0.0 в `install.sh`, `preflight` и `doctor` (до 28 loopback-порты были видны соседям по L2), `up`/`restart` отказываются без `nodeui-pass` (иначе нода молча ставит `mystberry`), `--tequilapi.allowed-hostnames=.` убран (отключал защиту от DNS-rebinding), `nodeui-pass` прежней роли откладывается в `.bak`, факты о секретах в `doctor` потрёхзначные по файлам
- [x] **nftables baseline VPS** — ✅ (2026-09-12, `next`) секция `firewall` (только vps: `ssh_ports` из `sshd_config` через `init`, `allow_tcp/udp`), шаблон `firewall.nft.j2` → таблица `inet vibedpn` с input policy drop (ssh, wg, UDP ноды, `wg0`, ICMP), `engine/router.py` рендерит, проверяет `nft -c`, применяет одной транзакцией идемпотентно, отказывается без правила для ssh; `core` применяет при старте и падает с EX_CONFIG при ошибке; `doctor` проверяет загруженность таблицы; CI применяет ruleset дважды в контейнере с NET_ADMIN
- [x] **Ревью nftables-baseline (2 линзы + скептики)** — ✅ (2026-09-12, `next`) порты sshd читаются как читает sshd (пробел, таб, `=`, кавычки, `Match\t`), учитывается `ListenAddress host:port`, `init` под sudo спрашивает `sshd -T`, а файлы — запасной путь; `doctor` сверяет реальные порты sshd из `ss` с `firewall.ssh_ports` (`[FAIL] ssh`) и предупреждает о таблице, оставшейся после `enabled: false`; `core` снимает таблицу при старте, когда файрвола в конфиге нет (`add table` + `delete table`, CI проверяет); ответы DHCPv6 пропущены явно (conntrack их не связывает); `install.sh` ставит `nftables` на хост, чтобы `doctor` видел таблицу; опровергнуто: диапазон из одного порта nftables ≥ 1.0.6 принимает
- [x] **`GET /provider/stats` через TequilAPI; `vibedpn status` показывает состояние ноды** — ✅ (2026-09-12, `next`) `engine/myst.py`: клиент TequilAPI на httpx (loopback, без учётных данных, таймаут 2 с на запрос) и чистая сборка `ProviderStats` из `/healthcheck`, `/identities`, `/identities/{id}`, `/services`, `/sessions/stats-aggregated`, `/node/monitoring-status` — терпимая к пропущенным и криво типизированным полям; `GET /provider/stats` в core (404 без провайдера в роли, 503 с текстом причины, когда нода не отвечает); `vibedpn status` ходит в core, а не в ноду, и печатает блок `node` (версия, аптайм, мониторинг, identity с балансом и заработком в MYST, сервисы, итоги сессий) или одну строку почему блока нет; тесты на ответах, записанных с живой 1.39.5, и на «занятой» ноде по swagger. Ревью (2 линзы + скептики): `/identities` отдаёт только `id` — статус и балансы берутся из `/identities/{id}`; обязателен только `/healthcheck`, остальное деградирует к дефолтам с причиной в `problems` (мониторинг ходит во внешний оракул); CLI отличает «core не запущен» (ConnectError) от «core не ответил» и не падает на чужом ответе 200; таймауты выровнены (нода 2 с × 6 запросов < ожидание CLI); loopback-клиенты не уважают `HTTP_PROXY`. Заодно ревью нашло в baseline файрвола high: `flush table` не чистит элементы set — правка `provider.udp_ports` с пересечением роняла core в рестарт-цикл, без пересечения оставляла старый диапазон открытым; транзакция стала `add table` + `delete table` + таблица, CI применяет суженный диапазон
- [ ] Проверка: нода видна и заклеймлена в mystnodes.com, инструкция клейма в README

## Stage 3 — Приватный туннель

- [x] **Образ `vibedpn/wg`, entrypoint `server|client`, kill-switch в режиме client** — ✅ (2026-09-12, `next`) entrypoint вместо wg-quick: вырезает ключи, которых не знает `wg setconf` (`Address`, `MTU`, `DNS`, `Table`, хуки), поднимает wg0 через netlink, раздаёт адреса и маршруты по `AllowedIPs`, при `0.0.0.0/0` — fwmark 51820 с таблицей 51820 и `suppress_prefixlength 0`; в режиме client — kill-switch и MASQUERADE таблицей `inet vibedpn_wg` в netns самого контейнера (хост не трогаем); интерфейс упал (удалён или down) — контейнер выходит ненулевым кодом и `restart: unless-stopped` поднимает туннель заново, утечки в промежутке нет; healthcheck `entrypoint.sh health` (client — свежий handshake, server — поднятый wg0); тест `tests/wg/tunnel.sh` — два контейнера, живой handshake, трафик в туннеле, kill-switch и самовосстановление; в CI задача `tunnel` (прошла на раннере GitHub с его модулем `wireguard`), образы публикуются только после неё. Ревью (2 линзы + скептики): остаток `wg0` в netns хоста ронял сервер в вечный рестарт после SIGKILL — интерфейс пересоздаётся; метка `FwMark` идёт из конфига, а таблица nft ставится до интерфейса (иначе сплит-туннель жил только на гонке и conntrack); читаются все строки `Address` и конфиги с CRLF; клиенту без `PersistentKeepalive` он проставляется; сторож клиента следит и за возрастом handshake (сменившийся адрес VPS резолвится при рестарте); сервер отказывается стартовать с `AllowedIPs 0.0.0.0/0`, чтобы не переписать маршрутизацию VPS; тест дополнен сплит-туннелем, CRLF-конфигом, падением сервера в host-netns и защитой от «проверка прошла, потому что контейнер мёртв»; заодно в CI прибита версия `shellcheck` с проверкой sha256 — у раннера она отставала и ругалась на код, который локальная пропускала
- [x] **`wg-server` на VPS (wg0 10.78.0.1/24), ключи в `core`, персист в volume** — ✅ (2026-09-12, `next`) `engine/wg.py`: ключ сервера X25519 (`cryptography`, clamping как у `wg genkey`, проверено вектором RFC 7748) генерируется один раз в `secrets/wg-server/server.key`, `wg0.conf` рендерится из секции `wg_server` шаблоном `wg-server.conf.j2` при каждом старте `core` (адрес — первый хост подсети, рендерер уже принимает пиров); запись атомарная с правами 600 в каталоге 700, неизменившийся файл не перезаписывается, нечитаемый ключ — отказ старта, а не новый ключ; `wg-server` ждёт здорового `core`; `doctor` проверяет наличие и права файлов; общее окружение шаблонов вынесено в `templating.py`. Smoke на Linux-VM: открытый ключ живого wg0 совпадает с `wg pubkey` от сохранённого файла, `restart` сохраняет ключ, смена `listen_port` доезжает до wg0. Smoke же нашёл, что `compose restart` перезапускает всё разом и не соблюдает `condition: service_healthy` — сервер читал ещё не перерендеренный конфиг; `vibedpn restart` теперь `stop` + `up`. Ревью (2 линзы + скептики, подтверждено два low): запись ключа и конфига теперь с `fsync` файла и каталога — без него сброс питания сразу после первого старта мог оставить пустой `server.key`, который ядро справедливо отказывается заменять (`auto_da_alloc` ext4 страхует только замену существующего файла); тест порядка старта видит шаг туннеля между файрволом и API, отдельный тест держит `depends_on` в compose. Из low: после перезагрузки Docker поднимает контейнеры без `depends_on`, поэтому сторож entrypoint пересобирает туннель, когда `wg0.conf` сменился (проверено `tunnel.sh` на сервере в netns хоста); поведение fail-closed у `restart` при неподнявшемся `core` описано в мануале
- [x] **`vibedpn peer add|rm|list|export` → `.conf` + QR в терминале** — ✅ (2026-09-12, `next`) реестр пиров `secrets/wg-server/peers.json` (права 600, пишет только `core`, под блокировкой, атомарно; повреждённый — отказ, а не сброс): имя `[a-z0-9-]`, первый свободный адрес после сервера, понятная ошибка при кончившейся подсети, удаление освобождает адрес; клиентский `.conf` (`/32`, `Endpoint` из `wg_server`, `AllowedIPs 0.0.0.0/0`, keepalive 25), который принимает `init --peer-config`; API `GET/POST /peers`, `GET /peers/{name}/config`, `DELETE /peers/{name}` — закрытый ключ пира только в выгрузке файла; CLI ходит в ядро (`secrets/` root-only): `add` и `export` печатают файл с QR (`segno`) или пишут `--out` с правами 600, `rm` спрашивает подтверждение, `list` показывает handshake, трафик и endpoint из `wg show wg0 dump` (в образ ядра добавлен `wireguard-tools`). Сервер применяет изменение пиров на месте через `wg syncconf` с синхронизацией маршрутов, перезапуск — только при смене `[Interface]`. Smoke на Linux-VM: `peer add`, клиентский контейнер с выгруженным файлом пингует 10.78.0.1, `peer list` видит handshake, `peer rm` отрезает его, `wg-server` ни разу не перезапустился. Smoke же нашёл, что хеш `[Interface]` захватывал пустую строку перед первым `[Peer]` и сервер перезапускался на первом пире; хеш теперь по нормализованным строкам, а `tunnel.sh` проверяет пустой сервер — на прежнем варианте проверка падает. В roadmap добавлен пункт про NAT пиров на VPS. Ревью (2 линзы + скептики, подтверждены medium и low): смена `wg_server.subnet` молча ломала всех пиров — ядро при старте переносит их в новую подсеть с прежними ключами и пишет в лог, кого куда (smoke: `flat` с 10.78.0.2 на 10.99.0.2, выгрузка с тем же ключом), подсеть мала для реестра — ошибка старта с числами; реестр и `wg0.conf` пишутся одной операцией с откатом конфига; сбой записи — понятная ошибка, а не трейсбек; выгрузка файла только читает ключ сервера и не создаёт новый; `--out` отказывается писать через симлинк и заменяет файл через временный 600 и rename (`atomic.py`). В пункт про доступ через wg0 записано ограничение: управление пирами и выгрузка ключей остаются на loopback
- [x] **NodeUI 4449 и `core` API доступны с домашней стороны через wg0 и больше ниоткуда** — ✅ (2026-09-12, `next`) ядро на роли vps слушает адрес сервера туннеля через `IP_FREEBIND` (слушатели поднимаются раньше `wg0` и переживают его пересоздание — проверено исполнением): на `api.port` за шлюзом ASGI только `GET /health` и `GET /provider/stats` и только с адресов туннеля, управление пирами и выгрузка ключей остаются на loopback; на 4449 — TCP-проброс на панель ноды (TequilAPI через туннель не выходит). Несколько uvicorn в одном процессе с одним обработчиком SIGTERM. Файрвол: с `wg0` вместо «всё» — только 4449 и порт API, пакеты к подсети туннеля не через `wg0` — drop (CI проверяет правила и отсутствие старого). `doctor` проверяет слушатели на адресе туннеля. Добавлен `peer add --tunnel-only` (в файле `AllowedIPs` только подсеть туннеля — для ноутбука или телефона), `peer list` отличает ещё не применённого пира (`pending`) от недоступного интерфейса. Smoke на Linux-VM: пир получает `/health`, `/provider/stats` и страницу панели по 10.78.0.1, `/peers` — 404, посторонний контейнер — 403 и обрыв, после перезапуска `wg-server` API вернулся за ~6 с. С домашней LAN адрес туннеля станет доступен через шлюз в Stage 4. Ревью (2 линзы + скептики; подтверждена одна, понижена до low): проброс не закрывал соединение молча пропавшего клиента — теперь таймаут простоя на чтение и запись, пауза 30 с после полузакрытия, TCP keepalive, не больше 128 проброшенных соединений и 64 запросов к туннельному API; все сокеты открываются до запуска, занятый порт — одна строка при старте; `serve()` целиком с остановкой по SIGTERM покрыт тестом; `doctor` сверяет процесс-владельца (`vibedpn-core` в `ss`) и предупреждает без `ss`. Опровергнуто исполнением: через проброс панели TequilAPI отвечает 401 без входа. Smoke: ядро останавливается за 0 с при живом проброшенном соединении
- [x] **VPS выпускает трафик пиров в интернет** — ✅ (2026-09-13, `next`) отдельная таблица `inet vibedpn_egress` (не `inet vibedpn`: NAT нужен и при `firewall.enabled: false`): forward с policy accept — адрес туннеля не с `wg0` отбрасывается, новые соединения из интернета в `wg0` тоже; postrouting — masquerade подсети туннеля не в `wg0`. Исполнением и по документации: `accept` в своей nft-таблице не отменяет `-P FORWARD DROP`, который ставит Docker, — поэтому ядро ставит два правила в `DOCKER-USER` (пиры наружу и ответы обратно, метка `vibedpn-egress`), приводя их к нужным по `iptables -S`: старая подсеть и дубли удаляются, недостающие вставляются в начало, чужие правила не трогаются; бэкенд `iptables-nft`/`iptables-legacy` — тот, где есть цепочка, без цепочки (nftables-бэкенд Docker) хватает таблицы. В образ ядра добавлен `iptables`. Без сервера туннеля в конфиге таблица и правила снимаются. `doctor` — проверка `tunnel egress` (таблица, правила в `DOCKER-USER`, без root — warn). CI `firewall`: таблица, правила дважды без дублей, смена подсети, снятие с сохранением чужого правила. Smoke на Linux-VM (Docker 29.5.2): пир выходит в интернет под адресом VM, без наших правил 1.1.1.1 недоступен и `doctor` — fail, перезапуск ядра и `dockerd` возвращают правила и выход. Ревью (2 линзы, обе нашли одно medium): правило `! -o wg0` пускало пира на неопубликованные порты контейнеров VPS мимо проверок Docker — в таблице теперь drop от подсети туннеля в `docker0`/`br-*` и в link-local (метаданные облака), RFC 1918 и CGNAT (smoke: контейнер VPS пиру недоступен); наши правила в `DOCKER-USER` считаются на месте только первыми в цепочке — под чужим DROP/RETURN переставляются наверх; снятие сначала убирает правила `DOCKER-USER`, потом таблицу; `doctor` сверяет подсеть в загруженной таблице с конфигом

## Stage 4 — Роль client, sidecar, аплинк vps

- [x] **`wg-client` как gateway-контейнер (10.77.0.10) с ip_forward + MASQUERADE + kill-switch** — ✅ (2026-09-13, `next`) адрес в `vibedpn-upstreams`, `ip_forward`, MASQUERADE и kill-switch в netns контейнера сделаны ещё в Stage 3 (образ `vibedpn/wg`); теперь шлюз подтверждён транзитом и дополнен подрезкой TCP MSS: первым правилом цепочки `forward` — `tcp flags syn tcp option maxseg size set rt mtu` (у LAN MTU 1500, у туннеля меньше — без подрезки большие TCP-сегменты пропадают там, где режут ICMP); та же подрезка в обе стороны добавлена на VPS в `inet vibedpn_egress`. `tests/wg/tunnel.sh`: хост LAN без своего WireGuard с маршрутом по умолчанию через клиента пингует сервер, счётчики nft на сервере видят его SYN с адреса клиента в туннеле (MASQUERADE) и ни одного с MSS больше 1360 (MTU 1400 из файла пира); сосед по сети напрямую отвечает, через шлюз — нет (шлюз пересылает только в `wg0`). Прогон на Linux-VM: `TUNNEL-OK`; на образе без строки подрезки тот же тест падает ровно на MSS (проверка не проходит по ложной причине). Грабли теста: `docker exec` без `-i` отдаёт `nft -f -` пустой stdin и таблица молча не создаётся, а пустой счётчик проходил бы проверку — нечитаемый счётчик теперь FAIL. CI-сценарий `firewall` с новой строкой шаблона — `nft` принимает правило
- [x] **`engine/router.py`: nft-шаблоны, fwmark, `ip rule`, таблица `vps`; режимы `off` и `full`; идемпотентный apply** — ✅ (2026-09-13, `next`) таблица `inet vibedpn_router`: в `full` трафик LAN (не в LAN, не в сеть шлюзов, не на адреса коробки) получает метку аплинка (`vps` 0x10 → таблица 7710 → 10.77.0.10, `dpn` 0x20 → 7720 → 10.77.0.20, мост `vibedpn0` через `com.docker.network.bridge.name`), `ip rule` с явным приоритетом; прямой доступ LAN в 10.77.0.0/24 и пересылка IPv6 из LAN отброшены, ICMP redirect в LAN не уходят (sysctl из `core` недоступен — `/proc/sys` только для чтения), MASQUERADE прямого пути на одном порту; транзит через Docker — группа `DOCKER-USER` `vibedpn-router` (группы `vibedpn-*` делят верх цепочки), новые соединения из шлюзов в LAN запрещены. Kill-switch: при `failopen: false` в таблице аплинка `unreachable default` с худшей метрикой; наблюдатель в `core` раз в 5 с проверяет шлюз ICMP (raw-сокет) и ставит или снимает его маршрут — остановленный контейнер оставляет мост, и без этого трафик тонул бы в ARP. Порядок: kill-switch → новое правило → новые метки → снятие старых правил по полному селектору. `core` применяет роутер при старте, без LAN снимает; в образ ядра добавлен `iproute2`. `doctor`: проверка `router` (таблица, правила, `DOCKER-USER`, маршрут kill-switch, шлюз, строгий `rp_filter`). CI-задача `router` в контейнере с NET_ADMIN. Smoke роли client на Linux-VM (LAN — мост с устройством в netns, стенд VPS — контейнер `wg`): в `full` трафик устройства идёт через туннель (байты на стенде растут), 10.77.0.10 отвечает коробке и молчит устройству, остановленный шлюз при `failopen: false` отрезает устройство и возвращает с ним, при `true` — выход напрямую мимо туннеля, в `off` правил нет и туннель не используется; прямой путь на одном порту стендом не воспроизводится (L2 с `eth0` VM недоступен) и проверен только правилами. Ревью (2 линзы): окно утечки при смене аплинка, маршрут, снятый извне, строгий `rp_filter`, проверка kill-switch в `doctor`, соединения из шлюзов в LAN, IPv6 — исправлены; TequilAPI через tun `myst-consumer` и IPv6 через роутер провайдера — пункты Stage 8 и AdGuard; доступ устройств LAN к панели VPS — вопрос владельцу (knowledge `linux/lanRouter.md`). Попутно найдено и исправлено в шлюзе `wg-client`: ответы устройствам LAN из другой подсети уходили в туннель — теперь соединения не из туннеля метятся в conntrack и их ответы идут через основную таблицу; `tunnel.sh` моделирует LAN за роутером и на прежнем образе падает
- [x] **nft: LAN не достигает адресов `10.77.0.0/24` напрямую (только транзит через шлюзы); транзит через FORWARD разрешён явно** — ✅ (2026-09-13, `next`) сделано в коммите роутера `179354a`: `iifname <lan> ip daddr 10.77.0.0/24 drop` в `inet vibedpn_router` (drop в nft окончателен, `ACCEPT` из `DOCKER-USER` его не отменяет), транзит LAN ↔ `vibedpn0` — группа `DOCKER-USER` `vibedpn-router`; следствие `nat-unprotected` ([knowledge/docker/directRouting.md](knowledge/docker/directRouting.md)) закрыто. Доказательства дополнены: CI-задача `router` проверяет правило в загруженной таблице, smoke на Linux-VM — слушатель на 10.77.0.99:4050 (как TequilAPI) открыт коробке и закрыт устройству LAN по TCP, 10.77.0.10 по ICMP так же. Адрес моста 10.77.0.1 на хосте локален и открытости не добавляет (`core` и порты ноды — на 127.0.0.1); доступ через tun-адрес `myst-consumer` — пункт Stage 8
- [x] **Авторизация `/api/` в `ui` (nginx `auth_basic`, пароль из `init`)** — ✅ (2026-09-13, `next`) `location /api/` за `auth_basic` с `secrets/htpasswd` (bcrypt, пользователь `admin`), страница интерфейса открыта (экран входа — Stage 6). Исполнением: `auth_basic_user_file` читает рабочий процесс nginx на каждом запросе, root-only файл давал 500 на любой пароль, а `mode` секретов Compose для файлов игнорируется — скрипт в `/docker-entrypoint.d` копирует файл при старте в `/etc/nginx/htpasswd` с `root:nginx 0440`, права `secrets/` на хосте не меняются; без файла, с пустым или каталогом вместо него контейнер не стартует. Монтирование без создания пути на хосте. CI-задача `ui-auth`: 401 без пароля, 200 с верным, 401 с неверным и с чужим именем, 200 на `/`, отказ старта без пароля (прогнано на Linux-VM). Отдельная проверка в `doctor` не понадобилась: `htpasswd` уже обязательный секрет каждой роли, `doctor` падает на его отсутствии
- [x] **`vibedpn mode off|full`, `vibedpn upstream vps`** — ✅ (2026-09-13, `next`) команды меняют `routing.mode` / `routing.default_upstream` в `config.yaml` round-trip-правкой `ruamel` с отступами шаблона: меняется ровно одна строка, комментарии владельца остаются (тест на рендерах client и home); результат проверяется полной моделью до записи — значение, с которым коробка не стартует (`upstream dpn` без включённого `dpn`, `full` через выключенный аплинк), отвергается без записи; `smart` не принимает сам разбор аргументов; роль без `routing` — отказ. Запись атомарная с правами и владельцем файла (`atomic.write_like`); каталог коробки принадлежит root, поэтому без sudo — отказ с подсказкой, а не запись на месте. На поднятой коробке перезапускается только `core` (`compose restart --no-deps core`: роутер применяется при старте ядра, `.env` не меняется), иначе изменение применится при `up`; повтор того же значения ничего не трогает. `status` и команды печатают одну строку `routing:`. Smoke роли client на Linux-VM: `mode off` — трафик устройства мимо туннеля (байты стенда не растут на серии из трёх запросов), `mode full` — снова через туннель (5856 → 7824), `wg-client` не перезапускался, `status` показывает режим, повтор — «already set». Критерий smoke: одинаковая серия запросов устройства, а не угаданный порог по одному запросу
- [x] **AdGuard Home на 53 LAN-интерфейса, DoH-апстрим; README: как указать шлюз/DNS на устройстве и на типовом роутере** — ✅ (2026-09-13, `next`) `engine/adguard.py`: ядро при старте (до AdGuard — `depends_on`) пишет минимальный `AdGuardHome.yaml` (схема 34), а в существующем round-trip-правкой меняет только свои ключи — `http.address` (LAN-адрес и `dns.web_port`), пользователя `admin` с bcrypt-хешем из `secrets/htpasswd`, `dns.bind_hosts` (только LAN-адрес — заглушка systemd-resolved не мешает), `dns.port: 53`, `upstream_dns` из `dns.upstreams`; фильтры и настройки из UI AdGuard и чужие пользователи остаются, файл чужой схемы — отказ старта ядра без записи. Исполнением на v0.107.79: мастер не появляется, хеш `$2b$` принимается (вход 200/403), первый старт переписывает файл целиком (600, умолчания), дальше не трогает; `ruamel` писал `None` пустым значением — представление задано явно (поймал тест на реальном переписанном файле). В `core` смонтирован `data/adguard/conf`. README: раздел «Как направить устройства через коробку» — вручную и DHCP-опциями 3 и 6 роутера, пример OpenWrt по его документации. CI-задача `adguard`: файл от ядра на прибитом образе, вход паролем, пустые AAAA в `full`, фильтр из UI переживает проход ядра. Smoke роли client на Linux-VM: устройство LAN резолвит через AdGuard на адресе коробки, вход паролем `init`
- [x] **Режим `full` на одном порту: IPv6 устройств мимо коробки — пустые AAAA и предупреждение `doctor`** — ✅ (2026-09-13, `next`) `dns.aaaa_disabled` = режим `full` (исполнением: AAAA пустой, A обычный); `vibedpn mode` перезапускает и `adguard` — после того как `core` снова здоров (`compose up -d --no-deps --wait core`): вместе AdGuard выигрывал гонку и стартовал на старом файле, smoke это поймал (в `off` AAAA оставался пустым). `doctor`: в `full` проверка `ipv6` — глобальный IPv6 на `lan_interface` (`ip -j -6 addr`) даёт WARN с подсказкой выключить RA/DHCPv6 на роутере. Smoke: `full` — AAAA 0, `mode off` — AAAA 4, снова `full` — 0
- [x] **E2E-стенд `tests/e2e`: fake-vps + box + lan-client; тесты exit IP / mode off / kill-switch** — ✅ (2026-09-13, `next`) `tests/e2e/router.sh` на одном Linux-хосте поднимает коробку роли `client` из образов репозитория (`vibedpn init/up/mode/doctor`, AdGuard), fake-VPS (`vibedpn/wg` server с NAT) и устройство LAN в netns; «интернет» — своя Docker-сеть 198.18.0.0/24 с веб-сервером, отвечающим адресом клиента, так что путь доказывается адресом выхода, а не счётчиками туннеля. Проверки: `full` — выход с адреса VPS; сеть шлюзов закрыта устройству по ICMP и TCP и открыта коробке; остановленный шлюз при `failopen: false` — выхода нет, при `true` — выход напрямую; `vibedpn mode off` — напрямую без перезапуска `wg-client`, `mode full` — снова через VPS; `doctor router`; AdGuard — резолв, пустые AAAA в `full`, вход паролем `init` (DNS-проверки выключает `VIBEDPN_E2E_OFFLINE=1`). CI-задача `e2e` (Docker 28.0.4 на раннере проходит порог продукта), раздел в `manuals/devSetup.md`, knowledge `ci/e2eStand.md`. Грабля первого прогона: сеть «интернета» в режиме `nat` Docker защищает от других интерфейсов хоста (`raw PREROUTING -d <IP> ! -i <мост> -j DROP`) — пакет устройства умирал до роутера; сеть создаётся с `nat-unprotected` (проверено пробой). Прогон на Linux-VM: 83 с, зелёный. Внешний smoke роутера вне репозитория удалён — всё, что он проверял, в стенде (кроме замера байт туннеля, от которого отказались). Одно-портовая LAN (роутер провайдера в L2 интерфейса хоста) стендом не воспроизводится
- [x] **`vibedpn doctor` v2: exit IP по каждому аплинку, утечка DNS** — ✅ (2026-09-13, `next`) флаг `vibedpn doctor --network` (без него `doctor` в интернет не ходит): `exit direct` — внешний адрес хоста, `exit vps|dpn` — адрес выхода изнутри контейнера-шлюза (`compose exec -T <сервис> wget` — busybox `wget` с TLS есть и в `vibedpn/wg`, и в `myst:1.39.5-alpine`, проверено исполнением); аплинк без ответа или с прямым адресом — FAIL (трафик мимо туннеля), хост без ответа — WARN. `dns leak`: в `full` при AdGuard — WARN, DoH-запросы уходят напрямую и провайдер DoH видит адрес коробки (публичные тесты утечки при DoH показали бы адрес Cloudflare — поэтому проверка структурная, до маршрутизации AdGuard через аплинк в Stage 11). Сервис адреса — ipify (по его политике ничего не логирует), `VIBEDPN_EXIT_IP_URL` направляет стенд на свой эхо-сервер: на одной машине с настоящим интернетом fake-VPS и хост выходят одним NAT, и сравнение было бы ложным. E2E-стенд проверяет: `exit direct` 198.18.0.1, `exit vps` 198.18.0.20, `dns leak` WARN. Проверки коробки с LAN вынесены из `evaluate` в `_lan_results`

## Stage 5 — Политики по устройствам

- [x] **Обнаружение устройств LAN (ARP/neighbour), имена, персист в SQLite** — ✅ (2026-09-13, `next`) `engine/devices.py`: разбор `ip -j -4 neigh show dev <lan>` — устройство, если у записи есть MAC, адрес из `lan_subnet`, это не сама коробка и состояние не `FAILED`/`INCOMPLETE`/`NOARP`/`NONE` (формат и состояния сняты исполнением на VM и по ip-neighbour(8)); коробка видит тех, кто ходит через неё как через шлюз или DNS, сеть не сканирует. Хранилище SQLite `/var/lib/vibedpn/devices.db` (MAC — ключ, последний адрес, PTR-имя, первое и последнее появление, версия схемы в `user_version`, база новее — отказ старта). Имя из `config.yaml` важнее PTR; PTR — через резолвер хоста, только для новых устройств и сменивших адрес, до транзакции. Фоновая задача ядра раз в 30 с, ошибка — одна строка в журнал. Read-only `GET /devices` и `vibedpn device list`. E2E-стенд: устройство из netns появляется в списке со своим MAC и сохраняет `first_seen` после перезапуска ядра. DHCP-аренды перенесены в Stage 9: в sidecar у коробки их нет
- [x] **nft-сеты `devices_vps|dpn|bypass|block`, override поверх режима** — ✅ (2026-09-13, `next`) восемь наборов в `inet vibedpn_router`: `devices_<политика>_mac` (`ether_addr`) и `devices_<политика>_ip` (`ipv4_addr`, только для устройств без MAC) из раздела `devices` конфига; в prerouting первым совпадением — `bypass` без метки даже в `full`, `block` меткой 0x30 (отбрасывается в `forward`, коробка устройству доступна), `vps`/`dpn` своими метками даже в `off`, последней — метка режима. Исполнением на VM: `ether saddr @набор` в `inet`/prerouting ловит маршрутизируемый трафик устройства, пустые наборы `nft -c` принимает. Роутер работает с набором «аплинков в использовании» (режим плюс политики): у каждого своё `ip rule`, таблица с kill-switch и наблюдатель шлюза, `doctor` проверяет все; порядок применения без окна утечки сохранён. CI-задача `router`: наборы и правило режима проверяются на реальном `nft`; E2E-стенд подтвердил, что режимы, kill-switch и `failopen` после перехода на несколько аплинков работают. Смена политики пока — правка `config.yaml` и `vibedpn restart`; команды — следующий пункт
- [x] **`vibedpn device set`, `GET/PUT /devices`** — ✅ (2026-09-13, `next`) `vibedpn device set <mac|ip> vps|dpn|bypass|block [--name]` и `vibedpn device unset` идут в ядро (`PUT`/`DELETE /devices/{id}`, без sudo): ядро round-trip-правкой меняет одну запись раздела `devices` (комментарии остаются, новая запись — по MAC или по адресу, имя из `--name`, PTR или `device-<хвост>`), проверяет результат полной моделью, пишет атомарно с правами и владельцем файла, применяет роутер на месте и сверяет наблюдателей шлюзов (аплинк, ставший нужным, получает наблюдателя, лишний — нет); роутер отказал — прежний `config.yaml` возвращается (503). Исполнением: `rename` поверх файлового bind-mount даёт `EBUSY`, поэтому `core` монтирует каталог коробки на запись (knowledge `docker/bindMountRename.md`). Конфиг держит `BoxState`, эндпоинты читают его текущим. E2E-стенд: `bypass` — выход напрямую, `block` — выхода нет, `unset` — снова через VPS, `core` не перезапускался
- [x] **E2E: два lan-client с разными политиками** — ✅ (2026-09-13, `next`) в `tests/e2e/router.sh` второе устройство (netns `e2e-lanhost2`, 192.168.77.3) на том же мосту; адрес выхода проверяется у обоих после каждого шага: `full` + второе `bypass` — 198.18.0.20 и 198.18.0.1; второе `block` — первое по-прежнему через VPS, второе без выхода; `off` + первое `vps` — первое через VPS, второе напрямую; остановленный шлюз при `failopen: false` отрезает только устройство с `vps`, сосед выходит напрямую; `unset` обоих и `full` — оба через VPS. Второе устройство видно в `vibedpn device list`. Помощники стенда получают netns устройства параметром. Политику `dpn` проверит Stage 8 вместе с `myst-consumer`. Прогон на Linux-VM зелёный; Stage 5 закрыт

## Stage 6 — UI v1

> **Решение владельца 2026-09-12 (уточнено тем же днём):** UI — полноценное приложение на start0
> (Point0 + Prisma + better-auth), не тонкий SPA; Point0 нравится владельцу, автор его развивает.
> Вариантов два, и оба ведутся параллельно, ни один пока не отсекается: полный — для N100,
> обрезанный — для Raspberry Pi; выбор — по ходу, когда оба будут в руках. Минимум функций:
> человеко-понятный мастер с выбором входящего и исходящего интерфейса для одной коробки.
>
> **Решение владельца 2026-09-13:** Raspberry Pi нет, проверка — на N100. Вариант `lite` принимается
> там же с памятью контейнеров как у Pi с 2 ГБ; поведение на Cortex-A72 — открытый риск, не условие.
> Бюджет памяти — вся коробка в 4 ГБ; под меньший объём оптимизируем только по запросу.

- [x] **План Stage 6: два варианта start0-приложения** — ✅ (2026-09-13, `next`) [uiVariants.md](uiVariants.md): Postgres, better-auth и Bun остаются в обоих; `lite` флагом сборки `UI_VARIANT` отключает воркер `pg-boss`, живые обновления backplane и тяжёлые экраны, Postgres с `shared_buffers=32MB`. Замер на VM aarch64 (пак v0.1.23): `docker build` на 2 ГиБ падает по памяти, нативная сборка — пик 2.18 ГиБ, поэтому образы только из CI (`ui-image`, buildx amd64+arm64, теги `-full`/`-lite`); runtime — приложение 205 МиБ в простое, 283 МиБ после 600 запросов, Postgres 69–78 МиБ, старт 290 мс, образ 469 МБ. Выбор — `VIBEDPN_UI_VARIANT`, `init` ставит `lite` при памяти меньше 4 ГиБ. Падение Bun на ARMv8.0 (Pi 4) исправлено в v1.3.9; приёмка `lite` на живом Pi 4 (порог 300 МиБ) — ждёт железо. Knowledge `frontend/start0Build.md`, `toolchain.md` помечен устаревшим
- [x] **Каркас start0-приложения (полный вариант), экран входа, только LAN** — ✅ (2026-09-13, `next`) пак start0 v0.1.23 вендорен в `ui/` руками (без `init.ts`, он делает `git init`); удалены демо `idea`, регистрация, сброс пароля и подтверждение почты, Google OAuth и привязка аккаунтов, письма Resend, админ-панель и профиль пака, openapi с отдельным паролем, страницы terms/policy; телеметрия необязательна в любом `HOST_ENV`. Вход: один пользователь `admin@vibedpn.lan`, `disableSignUp`, better-auth хранит маркер вместо хеша, `password.verify` сверяет пароль с bcrypt-строкой `admin` из `secrets/htpasswd` на каждом входе (`Bun.password.verify` принимает `$2b$` из Python bcrypt — проверено исполнением); аккаунт с другим хешем не входит. Rate limit включён явно, cookie `HttpOnly; SameSite=Strict` без `Secure` (HTTP в LAN). `/api/core/*` проксируется на ядро `127.0.0.1` только с сессией; прежний nginx с `auth_basic`, его entrypoint и заглушка удалены. Адрес — `bunServeConfig.hostname` из `UI_LISTEN_HOST` (у Bun по умолчанию `0.0.0.0`), в production обязателен. `compose.yaml`: `ui` на host-сети и `ui-db` (`postgres:17.11-alpine`, только `127.0.0.1:5480`, `POSTGRES_PASSWORD_FILE`); `init` создаёт `secrets/ui-db-password` и `ui-auth-secret` один раз, повторный `--force` их не трогает, `doctor` и `vibedpn up` требуют их при профиле `ui`. Dockerfile: `dist` на платформе сборки, runtime на amd64/arm64, entrypoint не стартует без секретов. CI: `ui` — Bun, `check`, unit и dom; `ui-auth` заменена на `ui-image`. Прогон нативного `dist` с Postgres и поддельным ядром: health 200, чужой адрес не отвечает, `/api/core` без сессии 401 и с сессией 200, неверный пароль 401, верный 200, регистрация 400, перебор — 429 со второй попытки, аноним на `/` — 302 на `/sign-in`, с сессией — 200. Сборка образа на colima 2 ГиБ невозможна — проверяет CI; `typedoc`-скриптов в v0.1.23 нет, чинить было нечего
- [x] **Вход в панель по имени коробки; лимит входа по настоящему адресу** — ✅ (2026-09-13, `next`) ключ `ui.host_name` (по умолчанию `vibedpn.lan`, проверка как у доменов `smart`) → `VIBEDPN_UI_HOST_NAME` → `trustedOrigins` панели рядом с LAN-адресом на той же схеме и порту; ядро держит в AdGuard одну свою перезапись имени на `network.lan_address` в `filtering.rewrites` с `enabled: true` (в v0.107.79 запись без него пропускается — по исходникам тега), последнее имя — в `data/core/adguard-host-name`, при переименовании прежняя запись снимается, перезаписи владельца остаются; `doctor` — проверка `ui name` (WARN без `dns`). Уязвимость, найденная прогоном: лимит better-auth читал `x-forwarded-for`, шесть неверных паролей с подменой — все 401; теперь middleware `/api/auth/*` удаляет пересылочные заголовки и ставит адрес сокета (`request.from.ip`), better-auth читает только его; правило входа — 5 попыток за 5 минут. Прогон `dist`: по имени 200, по чужому имени 403, по адресу 200, подмена `X-Forwarded-For`/`X-Real-IP` — 429; Origin без порта при порте 80 — 200. CI `ui-image` проверяет имя, чужое имя и подмену
- [ ] Обрезанный вариант для Raspberry Pi из той же кодовой базы (`UI_VARIANT=lite`) — сделано 2026-09-13, `next`: константа сборки `UI_VARIANT` (`server.env.consts`/`client.env.consts`), в `lite` нет воркера `pg-boss` и backplane сокетов (проверено по source map обеих сборок), `ui-db` с `shared_buffers=32MB`/`max_connections=20`; ключ `ui.variant`, `init` выбирает по `MemTotal` (< 4 ГиБ — `lite`) или `--ui-variant`, `doctor` — проверка `ui variant`; образы `-full`/`-lite`, `ui-image` на обоих. Замер на VM aarch64 2 ГиБ после 600 запросов: `full` 266+69 = 335 МиБ, `lite` 259+51 = 310 МиБ, соединений с базой 5 и 1. Решение владельца: бюджет — вся коробка в 4 ГБ, порог 300 МиБ на панель снят, оптимизация под меньшее — по запросу. Открыто: приёмка на N100 — роль `home` со всеми сервисами в 4 ГБ
- [x] **Экран статуса: аплинки, kill-switch, режим, переключатели** — ✅ (2026-09-13, `next`) ядро: наблюдатели аплинков хранят последний круг (шлюз отвечает, время, ошибка), `GET /status` отдаёт режим, аплинки с маршрутами шлюза и kill-switch (чтение маршрутов перенесено из `doctor` в `engine/router.read_routing`) и итог «LAN без выхода»; `PUT /routing` меняет `mode`/`default_upstream` через `BoxState` (файл, роутер на месте, откат) и сразу AdGuard — `disable_ipv6` через `POST /control/dns_config` от служебного пользователя `vibedpn-core` (пароль `secrets/adguard-core-password`, Basic; по исходникам v0.107.79). `vibedpn mode`/`upstream` идут через API без перезапусков (без ядра — правка файла), `vibedpn status` показывает шлюзы и итог. Панель: главная — итог, переключатели режима (full — с подтверждением) и аплинка, карточки аплинков; query за сессией, опрос раз в 5 с. CI `adguard` проверяет смену AAAA на лету и отказ чужому паролю; E2E-стенд — смену режима без перезапуска `core`
- [x] **Список устройств с политиками** — ✅ (2026-09-13, `next`) `GET /devices` отдаёт и устройства с политикой из `config.yaml`, которых обнаружение ещё не видело (`seen: false`, без адреса и времени; иначе политику, заданную по адресу заранее, нельзя было увидеть и снять), `vibedpn device list` пишет для них «never seen». Панель: страница «Devices» — таблица с поиском по имени, MAC и адресу, политика выбирается в строке (по режиму — `DELETE`, остальное — `PUT`; политика через выключенный аплинк недоступна, `block` — с подтверждением), своё имя — у устройства с политикой (API без политики имя не принимает), ошибка ядра — в строке; опрос раз в 30 с; пункты меню «Status» и «Devices»
- [x] **Переключатель в UI: доступ устройств LAN к панели ноды и API VPS через туннель** — ✅ (2026-09-13, `next`) решение владельца: по умолчанию закрыто. Ключ `upstreams.vps.lan_access`; адреса VPS в туннеле коробка не знает (в peer-файле только свой `/32` и `0.0.0.0/0`), поэтому при `false` роутер в `forward` отбрасывает трафик LAN с меткой `vps` ко всем частным диапазонам (`EGRESS_BLOCKED_RANGES`: дальше себя VPS пиров туда и так не пускает) — работает на уже установленных коробках без нового peer-файла, коробку не трогает. `PUT /uplinks/vps/lan-access` на лету через `BoxState`, `lan_access` в `/status`, переключатель в карточке VPS (открыть — с подтверждением), `doctor` — строка `vps lan access`. Проверено: `nft -c` на Alpine, тесты рендера и API; E2E-стенд — таймаут при закрытом, отказ сервера при открытом, без перезапуска ядра
- [x] **Вкладка ноды: статистика provider** — ✅ (2026-09-13, `next`) страница «Node» панели по `GET /provider/stats`: версия и время работы, мониторинг, сервисы, identity (регистрация, баланс, заработок — MYST из `wei` без плавающей точки), итоги сессий, вопросы без ответа; опрос раз в 30 с; без ноды (404) — объяснение, нода молчит (503) — причина. Только локальная нода роли `home`: нода VPS на коробке `client` недоступна, адреса VPS в туннеле коробка не знает — развилка в `docs/decisions.md`; роль `vps` панели не имеет

## Stage 7 — Роль home (all-in-one)

- [x] **Профильный пресет `home`: provider + consumer + router + dns + ui на одной коробке** — ✅ (2026-09-13, `next`) `init --role home` включает профили `provider,consumer,router,dns,ui` и пишет все пять секретов роли. Новый стенд `tests/e2e/home.sh` (своя LAN на мосту `lan0`, `--ui-variant full`, режим `full` через `dpn`) и CI-задача `e2e-home`: все сервисы роли поднимаются — ядро, панель и `ui-db` здоровы, AdGuard, `myst-provider` и `myst-consumer` работают. Прогон на VM colima (aarch64, 2 ГиБ) зелёный; память роли сразу после старта — около 326 МиБ (панель 118, ядро 63, AdGuard 45, `ui-db` 43, provider 38, consumer 20). Первый прогон поймал два дефекта стенда, не продукта: образа `lite` не было (на 2 ГиБ `init` выбрал `lite` — выбор по памяти отработал верно), порт 53 на адресе VM занят dnsmasq самой colima
- [x] **Проверка отсутствия петли: трафик provider уходит в WAN напрямую, не через аплинки** — ✅ (2026-09-13, `next`) по устройству роутер метит только трафик с `lan_interface` из `lan_subnet`, а нода — на своём мосту Docker; фактом на стенде `home`: при режиме `full` через `dpn` (таблица роутера метит LAN `0x20`) `myst-provider` выходит в интернет с тем же адресом, что и хост. В E2E-стенд клиента проверку не ставил: его «интернет» — тоже мост Docker, и изоляция Docker между мостами дала бы ложный провал
- [x] **Provider за NAT: инструкция по пробросу портов + вариант с natpunching, `doctor` проверяет** — ✅ (2026-09-13, `next`) README, раздел «Одна коробка»: проброс `provider.udp_ports` (по умолчанию 56000-56100) на адрес коробки, без проброса — `provider.traversal` (manual, upnp, holepunching). `doctor --network` — строка `nat` по `GET /nat/type` ноды (node 1.39.5: `none`/`fullcone` — OK, `rcone`/`prcone` — OK с советом пробросить, `symmetric` — WARN с командой проброса, нет ответа — WARN). Это оценка ноды, а не проверка проброса снаружи, и она только под `--network`: нода обращается к серверам Mysterium — развилка в `docs/decisions.md`
- [x] **README: сценарий «одна коробка»** — ✅ (2026-09-13, `next`) раздел «Одна коробка: всё сразу»: `init --role home`, `up`, направление устройств, панель на `vibedpn.lan`, нода за NAT и `doctor --network`, трафик ноды мимо туннелей

## Stage 8 — Аплинк dpn (Mysterium consumer)

- [x] **Ответы на вопросы §10 idea.md «Обязательно уточнить» зафиксированы в `docs/knowledge/myst/`** — ✅ (2026-09-13, `next`) `myst/consumer.md` по исходникам node 1.39.5 с URL: identity (`passphrase` обязателен), регистрация (`POST /identities/{id}/register`, бесплатно только при eligibility), `PUT /connection` (только `consumer_id` обязателен, фильтр страны), ловушка `kill_switch: true` = выключить kill-switch, баланс не проверяется при нулевой цене, свою ноду бесплатно — только раздавая её бесплатно всем (запрет самосоединения не найден, не проверено), официальный мультиарх-образ, `--data-dir`, автообновления нет. Поведение consumer в контейнере без host-сети — проверка запуском в следующем пункте
- [x] **`myst-consumer` gateway-контейнер (10.77.0.20): identity, регистрация, connect через TequilAPI, kill-switch** — ✅ (2026-09-13, `next`) образ `images/myst-consumer` поверх официального `mysteriumnetwork/myst:1.39.5-alpine`: kill-switch ноды (`MYST_CONSUMER_KILL_SWITCH`) по исходникам висит только на `OUTPUT`, поэтому обёртка ставит свою таблицу — транзит из `eth0` только в туннель, NAT, MSS clamp. Ядро раз в 30 с ведёт consumer (`engine/consumer.py`): создаёт и разблокирует identity парольной фразой из `secrets/myst-consumer-passphrase`, подключает зарегистрированную в `upstreams.dpn.country` без поля `kill_switch` (`true` его выключает), при смене страны разрывает сессию; блок `dpn` в `/status`. Регистрацию ядро само не запускает — `docs/decisions.md`, решение 4; пароль TequilAPI не вводится — решение 3. Стенд `home` на VM: ядро создало identity и видит `Unregistered`/`NotConnected` с подсказкой, устройство LAN без сессии без выхода, с `bypass` — с адресом хоста; первый прогон поймал таймаут 2 с на `GET /identities/{id}` (нода идёт в блокчейн) — стал 20 с. Утечка через штатный образ на стенде не воспроизвелась, причина не установлена (решение 5). **Не проверено:** реальное подключение — нужны регистрация и MYST
- [x] **TequilAPI `myst-consumer` недоступен из LAN и через адрес tun-интерфейса контейнера** — ✅ (2026-09-13, `next`) входной фильтр в контейнере: порт 4050 только с `lo` и с 10.77.0.1 (хост на мосту `vibedpn0`), с остальных — drop, в том числе с туннеля; хост и так режет LAN к 10.77.0.0/24. Стенд `home`: ядро дошло до TequilAPI и создало identity, устройство LAN до `10.77.0.20:4050` не достало
- [x] **Proposals → список стран, `PUT /dpn/country`, `vibedpn upstream dpn`** — ✅ (2026-09-13, `next`) `countries()` в `engine/consumer.py` группирует `GET /proposals?service_type=wireguard` ноды consumer по стране: число нод и минимальные цены за час и ГиБ в wei; `GET /dpn/countries` (404 без `dpn`, 503 если нода молчит), `PUT /dpn/country` через `BoxState` (код ISO, `null` — любая), `vibedpn dpn country <код|any>`; `vibedpn upstream dpn` уже шёл через API. Тесты группировки, смены страны на живой сессии, API и CLI; на реальных предложениях сети не проверено
- [x] **Таблица `dpn` в router-engine, переключение аплинка без разрыва bypass-устройств** — ✅ (2026-09-13, `next`) таблица 7720 с меткой 0x20 и порядок «новое правило → метки → удаление старого» — с Stage 4–5 (тест `test_switching_uplinks_never_leaves_a_mark_without_its_rule`). Фактом на стенде `home`: устройство с `bypass` выходило с адресом хоста во всех 12 пробах, пока режим переключался `full` → `off` → `full` через `PUT /routing`
- [x] **UI: выбор страны, баланс MYST consumer-identity** — ✅ (2026-09-13, `next`) карточка «Mysterium exit (dpn)» на экране статуса: регистрация, соединение, баланс (MYST из wei), адрес пополнения, выбор страны из `GET /dpn/countries` (число нод и минимальная цена за ГиБ) через `PUT /dpn/country`. Ядро отдаёт в блоке `dpn` `balance_wei` и `channel_address`; стенд `home`: `channel_address` пришёл от реальной ноды, баланс 0. Выбор страны в браузере не нажимался — проверены типы
- [x] **README: как пополнить consumer (Polygon), сколько стоит** — ✅ (2026-09-13, `next`) раздел «Как пополнить выход через Mysterium»: адрес пополнения — `channel_address` identity (до регистрации баланс identity — это MYST на этом адресе, `getUnregisteredChannelBalance` в node 1.39.5), MYST сети Polygon (контракт и где купить — по справочному центру MystNodes), регистрация бесплатна только при праве ноды, цены — у каждой ноды за час и за ГиБ (по умолчанию 0.00006 MYST/ч и 0.1 MYST/ГиБ), проверка баланса перед соединением. Реальный перевод не проверен — нужны средства владельца
- [x] **`channel_address` consumer-identity в `/status` и на экране статуса панели** — ✅ (2026-09-13, `next`) поле блока `dpn` и строка «Top up» в карточке; стенд `home` показал адрес от реальной ноды
- [ ] Команда регистрации consumer-identity для владельца (CLI и кнопка в панели): ядро само не регистрирует — `docs/decisions.md`, решение 4
- [ ] **Коробка платит сама за себя: provider оплачивает consumer** — 🧪 разведка (2026-09-17, `next`, knowledge `myst/selfFunding.md`): бесплатной регистрации сейчас нет (`eligible: false` у consumer и в пуле provider), `referral_token` нода считает бесплатной регистрацией; вывод заработка в свой канал нода запрещает, в канал consumer коробки — нет, это замыкает круг без биржи. Не проверено исполнением; неизвестны заработок домашней ноды, способ получить токен для раздачи и оплата первой регистрации
- [ ] **`myst-consumer` без сессии выпускает трафик напрямую** — найдено на коробке 2026-09-16 `doctor --network`: `[FAIL] exit dpn — uplink dpn leaves with the direct address` (утром тот же доктор давал «download timed out», то есть kill-switch держал; между замерами `compose pull` + `vibedpn up` пересоздали контейнер с новым образом `next`). Это утечка из решения 5, которая на стенде не воспроизводилась; `routing.mode` на коробке `off`, LAN через `dpn` не ходит. Разобрать: таблица nft обёртки внутри контейнера после пересоздания, порядок «таблица → туннель»

## Stage 9 — Gateway-режим (2 порта)

> **Решение владельца 2026-09-12:** целевое железо — N100 с одним LAN-портом и Wi-Fi: LAN-порт —
> входящий (WAN), Wi-Fi — точка доступа для домашних устройств. Пункт «Wi-Fi точка доступа» перенесён
> из «Предложений» сюда.

- [x] **`network.mode: gateway`: WAN/LAN интерфейсы, dnsmasq DHCP на LAN, NAT** — ✅ (2026-09-13, `next`) раздел `network.dhcp` (пул по умолчанию .100–.249 у /24, срок аренды, проверка: пул внутри подсети и без `lan_address`, подсеть не меньше /28); профиль Compose `dhcp` только в gateway; `engine/dnsmasq.py` рендерит `data/dnsmasq/dnsmasq.conf` при старте ядра (только DHCP, шлюз и DNS — `lan_address`); образ `images/dnsmasq` (Alpine 3.24, dnsmasq 2.92 — проверено сборкой), сборка в CI; роутер добавляет `masquerade` LAN → `wan_interface` и два правила транзита в DOCKER-USER; `doctor` — порт 67/udp и адрес LAN на интерфейсе. Решения 6–8 в `decisions.md`. Модульные тесты зелёные; на живом стенде проверит пункт E2E ниже
- [x] **DHCP-аренды dnsmasq как источник устройств и имён для обнаружения** — ✅ (2026-09-13, `next`) в gateway-режиме `api/discovery.py` каждый раунд читает `data/dnsmasq/leases` (через `engine/dnsmasq.core_dir()`, тот же каталог, куда ядро пишет `dnsmasq.conf`): живые аренды подсети становятся устройствами, адрес аренды важнее записи таблицы соседей, имя от DHCP-клиента важнее PTR, `*` — имени нет; нет файла — нет аренд. В sidecar аренд не читает. Модульные тесты на строках, снятых исполнением; стенд `gateway.sh`: устройство с арендой в `vibedpn device list`, строка клиента без имени — `*` (knowledge `linux/dnsmasqLeases.md`)
- [x] **`init` даёт выбрать LAN-интерфейс из обнаруженных** — ✅ (2026-09-13, `next`) `vibedpn init --lan-interface <имя>`: интерфейс с маршрутом по умолчанию — WAN, выбранный — LAN с адресом, который ему дала система (решение 7); `init` перечисляет интерфейсы с адресами (`HostProbe.interfaces()`, разбор `ip -j -4 addr show` на фикстуре), пишет `network.mode: gateway`, `wan_interface` и пул `dhcp` явно; интерфейс без адреса — отказ со списком годных, у `vps` флаг — ошибка; `.env` включает профиль `dhcp`. Мануал `installation.md` — раздел «Режим „в разрыв“». Стенд `gateway.sh` начинает с `init --lan-interface`
- [x] **UI даёт выбрать входящий и исходящий интерфейс из обнаруженных** — ✅ (2026-09-13, `next`) `GET /network` — сеть из `config.yaml`, интерфейсы хоста с адресами и отметкой маршрута по умолчанию, `restart_required`; `PUT /network {lan_interface}` — `null` возвращает один порт, имя — gateway с этим LAN (та же `network_for`, что у `init`), round-trip-правка раздела `network` с проверкой полной моделью, на месте ничего не применяется (решение 9). `BoxState.save` держит записанный конфиг отдельно от работающего: правка политики устройства после сохранённой сети применяет роутер со старой сетью. Страница панели «Network»: выбор из списка, после сохранения — «применится после `sudo vibedpn restart`» и новый адрес панели. Тесты ядра `test_network_api.py`, unit-тест выбора в панели
- [x] **Wi-Fi точка доступа (hostapd) как LAN-сторона gateway-режима** — ✅ (2026-09-13, `next`) раздел `network.wifi` (`ssid`, `country`, `band` 2.4/5, `channel` в пределах диапазона, `security` `wpa2-wpa3` по умолчанию или `wpa3`), только в gateway; профиль Compose `wifi`, образ `images/hostapd` (Alpine 3.24, hostapd 2.11), сборка в CI; `hostapd.conf` рендерит ядро при старте с паролем из `secrets/wifi-passphrase` (600). `init --lan-interface wlan0 --wifi-ssid --wifi-country` проверяет по sysfs, что интерфейс — радио, и генерирует пароль; `vibedpn wifi show`; `doctor` — `wifi`; `PUT /network` сохраняет `wifi` и пул `dhcp` неизменного LAN и отказывает переносить LAN с точкой доступа (попутно закрыта потеря своего пула DHCP при сохранении того же LAN). Решение 10. Стенд `tests/e2e/wifi.sh` на `mac80211_hwsim`: устройство подключилось по WPA3-SAE к смешанной сети паролем из `wifi show`, получило адрес по DHCP, вышло через NAT коробки, `doctor` — `[ ok ] wifi`; прогон на Linux-VM зелёный. Не проверено: настоящая карта N100 и 5 ГГц; CI-задача `e2e-wifi` на раннере не запускалась
- [x] **Свой пароль Wi-Fi: `vibedpn wifi passphrase`** — ✅ (2026-09-15, `next`) пароль спрашивается дважды без вывода на экран, проверяется по правилам WPA-PSK (8–63 печатных символа ASCII, без пробелов по краям — факт из примера `hostapd.conf` v2.11 в knowledge), пишется в `secrets/wifi-passphrase` с правами 600; тот же пароль ничего не перезапускает, новый пересоздаёт `core` (рендерит `hostapd.conf`) и `hostapd`. Тесты проверки, записи, перезапуска, отказа на коротком пароле и на коробке без Wi-Fi; на коробке не запускалось
- [ ] **Устойчивая точка доступа на RTL8852BE** — частично (2026-09-15, `next`): на коробке N100 телефон выкидывало каждые 6–60 с одновременно с `rtw89_8852be: timed out to flush queues`; выключатели энергосбережения драйвера не помогли; `ieee80211n=1`, `wmm_enabled=1`, `disassoc_low_ack=0` (`engine/hostapd.py` пишет их в каждый `hostapd.conf`, тест) сократили обрывы до раза в несколько минут (MCS 12, 78 Мбит/с), но в 10:22:39 UTC телефон снова выкинуло через секунду после той же ошибки драйвера. Затем ядро 7.1.8 и `firmware-realtek` 20260810 из `trixie-backports` (прошивка `rtw8852b_fw-2.bin`): обрывы реже — раз в 30–60 минут, каждый ровно в секунду `flush queues`. Не решено: корень — драйвер `rtw89` в режиме AP (knowledge `linux/hostapdConfigCheck.md`)
- [x] **Выход по готовому файлу WireGuard: `wg-<имя>`** — ✅ (2026-09-15, `next`) бесплатная замена платному выходу Mysterium: регистрация identity стоит около 0.1 MYST плюс газ Polygon (замер на коробке через транзактор ноды), а файл WireGuard не требует ни кошелька, ни биржи. Имена живут в `upstreams.wg`, файл — `secrets/wg-<имя>.conf`, сервисы рождает генератор `compose.wg.yaml` по образцу страновых потребителей; метка `0x50+i`, таблица `7750+i`, шлюз `10.77.0.50+i`, до восьми выходов. `routing.default_upstream` перестал быть перечислением и принимает ключ аплинка. Команда `vibedpn uplink add|rm|show`, спека в `configSpec.md`, рецепт с бесплатным тарифом — `manuals/wgUplink.md`, решение 23. Проверено: сгенерированный файл принят настоящим `docker compose config` (адреса 50/51, у каждого свой файл), команда прогнана на временной коробке (файл 600, отказ от негодного имени), тесты ядра зелёные. Попутно тесты поймали дефект: шаблон `config.yaml` не выводил новую секцию, то есть перерисовка конфига теряла бы выходы владельца — исправлено
- [x] **Журнал событий коробки: Wi-Fi и аплинки** — ✅ (2026-09-16, `next`) ядро подписывается на управляющий сокет hostapd и пишет в `data/core/events.db` подключения и уходы клиентов с длительностью сессии и включение и выключение точки; сторож аплинков пишет туда же смену «шлюз отвечает ↔ молчит». Событие хранит коды и числа, фразу собирают панель и CLI. API `GET /events`, `GET /wifi/clients`; CLI `vibedpn events`, `vibedpn wifi clients`; панель — страница «Журнал», блок Wi-Fi на странице сети, предупреждение на главной при трёх разрывах за сутки. Сокет в `/run/vibedpn/hostapd`, общий для двух контейнеров. Решение 24, knowledge `linux/hostapdControl.md`. Тесты: хранилище и его обрезка, разбор событий и станций, клиент на настоящем UNIX-сокете с поддельной точкой, фоновый цикл с длительностью уже идущих сессий, журнал аплинков, API, фразы CLI и панели
- [ ] **Обрывы Wi-Fi на rtw89: готовить отчёт разработчикам драйвера** — съёмка с включённой отладкой точки доступа (2026-09-15) сняла прежнюю гипотезу: `timed out to flush queues` сопровождает любую смену состояния станции, включая успешное подключение, и причиной не является. При настоящих обрывах в журнале нет ни одного кадра управления с обеих сторон (`inactivity`, `low ack`, `disassociated`, `deauthenticated`, `MLME-DISASSOCIATE` — по нулю): точка клиента не выгоняет, клиент не прощается, станция просто исчезает. Это потеря на уровне радио и драйвера. Частота при активном клиенте — раз в 30–45 минут. Разбор в `docs/knowledge/linux/hostapdConfigCheck.md`; дальше — собрать отчёт в linux-wireless с версиями ядра и прошивки, потому что сам по себе проект это не чинит
- [x] **Каналы `smart` на собственный выход WireGuard** — ✅ (2026-09-16, `next`, дополнение решения 23) правило или список `via: wg` с `uplink: <имя>`; набор `smart_wg_<имя>` (длина имени набора проверена на ядре коробки), метка выхода `0x50+i`, выход попадает в используемые — своя таблица, kill-switch и наблюдатель. API, `vibedpn rule add|lists add … wg --uplink`, запись в `config.yaml`, панель: канал «Через свой выход WireGuard» и выбор выхода, подпись в снифере. Попутно исправлено: выключенный выход получал контейнер и требовал файл. Тесты ядра и панели зелёные; живой проверки нет — на коробке нет файла выхода, а стенд `router` умеет только VPS
- [x] **Выход WireGuard из панели без SSH** — ✅ (2026-09-16, `next`, решение 26) страница «Выходы»: мастер со ссылкой на аккаунт Proton VPN и шагами, выбор файла или вставка текста, имя из имени файла; список выходов со снятием и строка «применяется / применено / ошибка». Ядро: `GET|POST /uplinks/wg`, `DELETE /uplinks/wg/{name}`, файл 600, ключ не отдаётся; запрос `data/core/apply-request` → юнит `vibedpn-apply.path` → `vibedpn apply` (`up`, итог, повтор для запроса, пришедшего во время работы). Юниты ставит `vibedpn up`. Проверка файла общая для CLI и панели и строже; снятие выхода удаляет файл. Тесты ядра и панели зелёные. Проверено на коробке тем же API, что у панели: выход с TEST-NET-адресом добавлен — файл 600, ключ в ответе не вернулся, юнит сработал, `vibedpn apply` поднял контейнер `vibedpn-wg-e2eprobe-1`, панель показала «применено»; снятие удалило файл и контейнер, доктор 20 ok. Первая выкладка нашла и починила две ошибки: у пакета не было `__main__` (падали бы и `vibedpn update`, и таймер обновлений), а упавшая служба оставляла панель на «применяется» навсегда
- [ ] **Proton Free с коробки** — 🧪 (2026-09-16, `next`) девять файлов бесплатных стран получены, проверены `read_peer_config`, лежат на коробке в `secrets/proton/` (root, 600); `proton-nl` подключён `uplink add`, `vibedpn up` поднял `wg-proton-nl`, `mode` остался `off`. Рукопожатия нет: инициации уходят (tcpdump на WAN), ответа нет на 51820 и восьми других портах, при этом адреса Proton пингуются и TCP 443 открыт, UDP к 1.1.1.1 работает — Ростелеком режет WireGuard к Proton (knowledge `wireguard/protonFree.md`). С телефона на мобильной сети Т-Мобайл (Android, приложение WireGuard, QR конфига CH) то же: 16 инициаций за 77 с без ответа — два оператора, блокировка общая. Выход `proton-nl` на коробке выключен (`enabled: false`, файл и запись оставлены на будущее). «Операторы режут» от «ключ не активен» отделит только рукопожатие из-за границы — машины нет. У бесплатного тарифа одно соединение — включённым держать один выход
- [x] **Бесплатный выход через Tor: аплинк `tor`** — ✅ (2026-09-17, `next`, решение 27) замеры с коробки (knowledge `tor/snowflake.md`): rutracker, lostfilm, nnmclub режутся по SNI и замирают после ~20 КБ даже с разрезанным ClientHello, Telegram и rutor закрыты по IP, obfs4-мосты Tor Browser не проходят, Snowflake проходит (~145 КБ/с). Аплинк `tor`: метка 0x60, таблица 7760, шлюз 10.77.0.60, образ `images/tor` (Debian trixie: tor, snowflake-client, obfs4proxy), TCP → TransPort, DNS → DNSPort, остальное отбрасывается. `upstreams.tor` (`enabled`, `bridges` — по умолчанию встроенные Snowflake Tor Browser), `vibedpn tor enable|disable|show`, канал `via: tor`, политика устройства `tor`, `vibedpn upstream tor`; API `GET/PUT /uplinks/tor`, карточка на странице «Выходы», каналы и политики в панели; доктор проверяет адрес выхода `tor`; образ в матрице CI. Проверено: на коробке шлюз в отдельной сети Docker открыл все цели для клиента с маршрутом через него; стенд `tests/e2e/tor.sh` на VM. Найдено стендом и исправлено: перенаправленные изнутри шлюза соединения отбрасывал фильтр (он видит прежний интерфейс), GNU wget повторял 20 раз — доктор висел
- [x] **Правила по сетям адресов: `routing.networks`** — ✅ (2026-09-17, `next`, решение 28) для приложений, которые ходят по адресу (Telegram): сеть с каналом, наборы `smart_<канал>_net` с интервалами, `direct` раньше выходов; API `GET/PUT/DELETE /networks`, `vibedpn net add|rm|list`; набор с девятью сетями Telegram проверен `nft -c` и загружен в контейнере
- [ ] **Выложить выход `tor` на коробку и настроить Telegram, rutracker, lostfilm** — ночью выкладку остановил классификатор разрешений; команды — в утреннем отчёте. После выкладки: проверка с телефона на Wi-Fi коробки
- [x] **Сети адресов в панели** — ✅ (2026-09-17, `next`) раздел «Сети адресов» на странице «Правила»: таблица с каналом и снятием, добавление с подсказкой до отправки (формат, биты хоста), кнопка «Добавить сети Telegram» — девять сетей IPv4 из core.telegram.org/resources/cidr.txt выбранным каналом; unit-тесты проверки сети
- [ ] **Ответы Mysterium через ретранслятор замирают после 16 КБ** — consumer получает 0 предложений: discovery отдаёт первые 16 КБ и молчит (knowledge `tor/snowflake.md`); разобрать, режет ли это ТСПУ по объёму на разрезанном соединении
- [x] **`doctor --network` проверяет адрес выхода и у `wg-<имя>`** — ✅ (2026-09-16, `next`) `_exits` опрашивает каждый включённый выход `upstreams.wg` тем же `wget` изнутри его контейнера; сервис по ключу даёт общий `uplink_service` из `engine/router.py` (свой словарь на два аплинка удалён), подсказка — `vibedpn logs wg-<имя>`. `gather` статит файлы `required_secrets(config)`, а не только известные `init`: файл выхода, заведённого позже, больше не даёт ложный WARN «cannot check … root-only» под `sudo`. Тесты: вердикты для `wg-<имя>`, опрос только включённых выходов, список файлов у сборщика. Найдено на коробке с `wg-proton-nl`: выход без рукопожатия доктор не показывал вовсе. Проверено на коробке после выкладки (`venv/bin/pip install ./core` — `pip` в PATH нет): `secrets` ok с `wg-proton-nl.conf`, `[FAIL] exit wg-proton-nl` (детали — «docker compose exited 137»: `wget` за kill-switch висит до таймаута `capture`, читаемее было бы «timed out»)
- [ ] **Сквозной тест выхода по файлу WireGuard** — поднять в стенде `router` второй сервер WireGuard и проверить выход через `wg-<имя>` в режиме `full` и через правило `via: wg` в умном режиме
- [x] **Правка настроек, которые ядро читает при старте, применяется `vibedpn up`** — ✅ (2026-09-16, `next`, решение 25) отпечатки разделов `config.yaml` в `.env` и в окружении `core`, `hostapd`, `dnsmasq`, `adguard`, `wg-server`; `up` пишет, что пересоздаёт. Проверено на коробке: второй DNS-апстрим и его снятие — оба раза `config.yaml changed for adguard, core`, по времени старта заново запущены ровно `adguard` и `core`, точка доступа, DHCP, выход и панель не тронуты; доктор 20 ok. Исходная постановка пункта — первая формулировка пункта (2026-09-15) была неверной: проверено на коробке 2026-09-16, `vibedpn up` без изменений не перезапускает ни одного контейнера, телефон остаётся на связи; всё роняет только `vibedpn restart`. Настоящая дыра другая: ядро при старте применяет файрвол, выход туннеля и основу роутера и рендерит `AdGuardHome.yaml` (`network`, `dns`, `ui.host_name`), `dnsmasq.conf`, `hostapd.conf` (`network.wifi`) и `wg0.conf` сервера. Для compose настройки ядра от этого не меняются, поэтому `up` его не пересоздаёт, и правка, например, имени сети Wi-Fi не применяется — остаётся `restart`. Нужно, чтобы `up` пересоздавал ровно ядро и те службы, чьи отрендеренные файлы изменились
- [x] **Раздающая нода выключена до регистрации identity** — ✅ (2026-09-15, `next`) на коробке `provider.enabled: false`, контейнер снят: без зарегистрированной identity нода не может опубликовать предложение, её собственный `GET /services` висит, и она выходит по таймауту своего же `POST /services` — перезапуск примерно раз в две минуты без какой-либо пользы. Выход (`myst-consumer`) это не затрагивает: 0 перезапусков. Вернуть обратно — после регистрации identity провайдера, разбор в `docs/knowledge/myst/sniBlocking.md`
- [x] **Смена пароля панелей: `vibedpn password`** — ✅ (2026-09-15, `next`) пароль спрашивается дважды без вывода и проверяется той же политикой, что в `init`; оба bcrypt-хеша (`secrets/htpasswd` строкой `admin` и `nodeui-pass` ноды) считаются до записи, файлы 600; панель читает htpasswd при каждом входе, `myst-provider` пересоздаётся, потому что NodeUI читает хеш при старте. Тесты: хеши проверяются bcrypt, повторный вопрос при коротком пароле
- [x] **Обход блокировки Mysterium без своей VPS** — ✅ (2026-09-15, `next`) провайдер рвёт TLS к `*.mysterium.network` по имени в первой записи (knowledge `myst/sniBlocking.md`); сервис `myst-relay` (образ ядра, `python -m vibedpn.relay`, `10.77.0.30` в сети шлюзов) принимает соединения нод к этим именам через `extra_hosts`, читает имя из ClientHello и пересылает байты на настоящий адрес, разрезая ClientHello на две TLS-записи там, где прямое соединение не проходит; ничего не расшифровывает, сертификат проверяет сама нода. Решение 22. Тесты: чтение имени, разрез с сохранением байтов, поддельный «провайдер» — ретранслятор сам переходит на разрез и запоминает выбор, чужая зона отвергается. Проверено на коробке: запрос к `observer` прошёл из сети шлюзов и с другого моста (сертификат проверен), а с включёнными нодами оба узла работают без перезапусков, `could not get hermes URL` исчезла, разрез включился для `transactor`, `location`, `pilvytis`, `hermes3`, `discovery`, `observer`, `quality`, `polygon1`; в логе названа причина перехода. Раздающая нода при этом всё равно перезапускается, пока её identity не зарегистрирована (её `GET /services` висит) — это экономика сети, не блокировка
- [x] **Панель на русском, свои языки файлами** — ✅ (2026-09-15, `next`) `ui.language` в `config.yaml` (по умолчанию `ru`) → `VIBEDPN_UI_LANGUAGE`; модуль `ui/src/modules/i18n`: английская база в бандле с плоскими ключами (`base.ts`, тип ключа — ошибка компиляции на неизвестный ключ), языки — файлы `data/ui/locales/<код>.json`, читаются один раз и накладываются на базу на сервере; язык запроса — cookie браузера, затем `ui.language`, затем английский, только если файл есть; переключатель в шапке, `<html lang>` и заголовки вкладок по языку, даты через локаль `date-fns`. Сводка статуса и чистые модули отдают ключи, фразу собирает экран. Образ кладёт `ru.json` в `/app/locales-seed`; entrypoint засевает отсутствующие файлы и обновляет те, что по-прежнему совпадают с засеянной копией (sha256 в `data/ui/locales/.seeded/`), а правленные владельцем и чужие файлы не трогает — иначе поставляемый язык застывал бы и новые строки шли по-английски (так и было на коробке с кнопкой глаза). Гейты: `react/jsx-no-literals` на экранах, тест каталога (те же ключи, те же подстановки, нет `en.json`, чтение папки с битым файлом). Спека `docs/manuals/languageFileSpec.md`
- [x] **Глаз у пароля на входе в панель** — ✅ (2026-09-15, `next`) кнопка в поле пароля показывает и прячет введённое (`aria-pressed`, подписи «Показать пароль» / «Скрыть пароль» из каталога): пароль коробки длинный, и его удобно сверить перед входом. Проверены типы, линт, тесты и сборка; на коробке после пересборки панели кнопка есть на странице входа (`aria-pressed`)
- [x] **Вход в панель по имени коробки** — ✅ (2026-09-15, `next`) панель, открытая по `http://vibedpn.lan`, слала вход и запросы на абсолютный `SERVER_URL` (`http://192.168.50.1:80`): сессионная cookie `SameSite=strict` ставилась адресу, а страница жила на имени, и после верного пароля вход не удерживался. Найдено перехватом на коробке: `Origin: http://vibedpn.lan`, `Host: 192.168.50.1`. В браузере production-клиент теперь говорит с тем origin, на котором открыт (`src/modules/env/client.ts`). Проверены типы, линт, тесты и сборка
- [x] **Стенд Wi-Fi в CI — в виртуальной машине Debian 13** — ✅ (2026-09-15, `next`) в ядре раннеров GitHub (Azure) нет `mac80211_hwsim`, и `e2e-wifi` падал на каждом push, не давая публиковать образы; `tests/e2e/wifiVm.sh` поднимает `debian-13-generic-amd64.qcow2` в QEMU с KVM (правило udev), ставит коробку тем же `install.sh` из проверяемого коммита, собирает образы и запускает `wifi.sh` без изменений. Две грабли первого прогона: в облачном образе нет `git` (ставится до `install.sh`, `safe.directory`), у обычного пользователя Debian нет `/usr/sbin` в `PATH` (`wifi.sh` добавляет сам). Прогон `34961786464`: все 19 проверок зелёные, в логе `E2E-WIFI-VM-OK` (knowledge `ci/e2eStand.md`)
- [x] **E2E: lan-client получает адрес по DHCP от box** — ✅ (2026-09-13, `next`) `tests/e2e/gateway.sh`: облегчённая коробка `home` в gateway (только `core` и `dnsmasq`, профили `router,dhcp`), LAN — мост с адресом, выставленным стендом как ОС; устройство в netns берёт аренду `dhcpcd`: адрес из пула, шлюз — коробка, строка аренды с его MAC в `data/dnsmasq/leases`; выход в интернет совпадает с выходом коробки без эмуляции NAT провайдера — значит через `masquerade` роутера; `doctor` — `[ ok ] lan address`. Прогон на Linux-VM зелёный, CI-задача `e2e-gateway` в `needs` образов

## Stage 10 — Smart-режим

> **Решение владельца 2026-09-14** (`docs/decisions.md`, 11): AdGuard впереди, домены правил — резолверу ядра;
> правило на домен (VPS, Mysterium со страной, напрямую), несколько стран сразу, CDN идёт за родителем, автообучение включено по умолчанию, снифер в панели и CLI.

- [x] **Правила доменов в `config.yaml`** — ✅ (2026-09-14, `next`) `routing.domains`: правило на сайт — `domain` (с поддоменами), `via` `vps`|`dpn`|`direct`, `country` только для `dpn`, `learn` (по умолчанию `true`), `also` — закреплённые CDN; один домен дважды — ошибка, канал правила должен быть включён; старый `smart_domains` читается как правила через `default_upstream`, вместе с `domains` — ошибка. Разбор кода страны — одна функция для `dpn`, Wi-Fi и правил. Шаблон и `configSpec.md`, пример перегенерирован; тесты и проверка «рендер → чтение» правил
- [x] **Резолвер ядра** — ✅ (2026-09-14, `next`) в smart апстрим AdGuard — резолвер ядра на `127.0.0.1:5354` для всех имён, DoH-серверы — запасные (решение 17: смена `upstream_dns` перезапускает DNS AdGuard, поэтому список доменов туда не пишется); резолвер спрашивает DoH через `dnspython[doh]`, до ответа кладёт адреса имени под правилом в набор канала одной транзакцией nft (обычный `add element` таймаут не продлевает), TTL таких ответов ≤300 с, элемент живёт на 60 с дольше; CNAME сайта ведёт CDN за сайтом; AAAA туннельных правил пустой; после каждого применения роутера наборы досыпаются из кэша ответов; любая ошибка — SERVFAIL. Проверено на VM: DoH-ответ, набор, UDP-сервер
- [x] **Несколько стран Mysterium одновременно (решения 16, 20)**:
  - [x] **набор стран из правил и их номера** — ✅ (2026-09-14, `next`) `rule_countries` — отсортированные страны правил `dpn`; `country_uplinks` — метка `0x40+i`, таблица `7740+i`, шлюз `10.77.0.40+i`; больше 8 стран — ошибка конфига. Тесты
  - [x] **`compose.countries.yaml` и секреты стран** — ✅ (2026-09-14, `next`) при `up`/`restart` пишется `compose.countries.yaml`: сервис `myst-consumer-<cc>` через `extends` из `compose.yaml`, свой адрес и каталог данных, без стран — `services: {}`; `compose_argv` передаёт `-f compose.yaml`, файл стран и `compose.override.yaml` владельца, если он есть; парольная фраза страны создаётся при первом запуске с ней и не заменяется, `check_secrets` её требует. Сгенерированный файл проверен настоящим Compose: `config -q`, сервисы стран в профиле `consumer`, адрес NL `10.77.0.41`
  - [x] **роутер: аплинк на страну** — ✅ (2026-09-14, `next`) ключ аплинка — строка (`vps`, `dpn`, `dpn-<cc>`), `uplink_table` собирает их из конфига; у страны своё `ip rule`, таблица с kill-switch и наблюдатель шлюза; `plan_rules` обходит все таблицы, включая свободные слоты стран, так что правило ушедшей страны снимается, а её таблица чистится; наблюдатели получают сам `Uplink` и перезапускаются при перенумерации; в smart набор `smart_dpn_<cc>` метится меткой своей страны, `smart_dpn_any` — меткой общего `dpn`; `doctor` называет сервис страны. Тесты
  - [x] **ядро ведёт consumer каждой страны** — ✅ (2026-09-14, `next`) каждый раунд идёт по общему `dpn` и по consumer каждой страны правил: свой TequilAPI `10.77.0.40+i:4050`, своя парольная фраза, подключение в свою страну, пока её правила в работе; статус по ключам `dpn`/`dpn-<cc>`, в `/status` — блок `dpn_countries` и шлюз страны среди аплинков; в панели — карточка страны с регистрацией, соединением, балансом и адресом пополнения, переключатель аплинка режима показывает только `vps` и `dpn`. Тесты цикла, статуса и типов панели; реальное подключение не проверено — нужны регистрация и MYST
- [x] **Снято временное ограничение решения 18** — ✅ (2026-09-14, `next`) проверка конфига удалена, правило `dpn` в smart принимает любую страну; заодно убран дубль `COUNTRY_PATTERN` в `config.py`. Тест
- [x] **`routing.mode: smart` в роутере** — ✅ (2026-09-14, `next`) наборы `smart_vps`, `smart_dpn_<страна|any>`, `smart_direct` с таймаутом; в smart адреса туннельных наборов метятся меткой аплинка (туннель раньше прямого при общем IP), остальное напрямую; `used_uplinks` берёт аплинки правил; `mode smart` в API и CLI, на лету. Временно (решение 18): правила `dpn` — только страна выхода Mysterium. Стенд `router.sh`: до запроса имени устройство выходит напрямую, после — адрес в `smart_vps` и выход через VPS
- [x] **«Иди за родителем»** — ✅ (2026-09-14, `next`) CDN за CNAME сайта идёт каналом сайта точно; CDN, который устройство спросило в окне 10 секунд после сайта под правилом с `learn`, выучивается с первого раза; имя, появившееся после больше чем трёх разных сайтов, — общее, снимается и не учится (решение 19); выученное хранится в `data/core/learned.db`, возвращается в резолвер после перезапуска ядра, снимается владельцем. Стенд `router.sh`: имя, спрошенное сразу после сайта, выучено за ним, `rule forget` вернул его на прямой путь
- [x] **Журнал DNS по устройствам, API снифера** — ✅ (2026-09-14, `next`) ядро читает `querylog` AdGuard раз в 2 с с курсором, последние 300 запросов каждого устройства в памяти с каналом, каким запрос ушёл в момент ответа; `GET /dns/devices`, `GET /dns/journal/{client}?since=`, `GET /learned`, `DELETE /learned/{name}`. Разбор проверен на записи настоящего AdGuard v0.107.79; стенд `router.sh`: запрос устройства к правилу виден в журнале с каналом `smart_vps`
- [x] **Снифер и редактор правил в панели** — ✅ (2026-09-14, `next`) страница «Rules»: таблица правил (канал, страна, автообучение, снятие закреплённого CDN, удаление с подтверждением) и добавление; снифер — устройства из `/dns/devices`, журнал выбранного раз в 2 с, кандидаты CDN (напрямую в течение 10 с после сайта с правилом) с кнопкой «Route like <сайт>», которая закрепляет имя в `also`; выученные CDN из `/learned` с «Forget»; без AdGuard или LAN — причина ядра вместо ошибки. Чистая логика (`ruleOf`, `cdnCandidates`, `withCdn`, `channelLabel`) покрыта unit-тестами; проверены типы и линт, в браузере страница не открывалась
- [x] **`vibedpn rule add|rm|list`** — ✅ (2026-09-14, `next`) ядро: `GET /rules`, `PUT /rules/{domain}` (добавить или заменить правило), `DELETE /rules/{domain}`; правка `routing.domains` в `config.yaml` с сохранением комментариев, полной проверкой модели и применением на лету через `BoxState` (ошибка правки — 422, нет правила — 404, отказ роутера — 503 с откатом); CLI `rule add <домен> vps|dpn|direct [--country] [--no-learn] [--also …]`, `rule rm`, `rule list`. Тесты API и CLI
- [x] **`vibedpn dns watch <устройство>`, `vibedpn rule learned|forget`** — ✅ (2026-09-14, `next`) живой журнал устройства по адресу или MAC, строка: время, тип, имя, канал, кэш, «выучено за сайтом». Команды проверены unit-тестами; на стенде проверены API, которые они читают, и `rule forget` исполнением
- [x] E2E: домен через VPS, напрямую, CDN по CNAME, CDN по времени, две страны — ✅ (2026-09-16, `next`) все подпункты проверены на стендах CI
  - [x] **домен через VPS и CDN по времени на стенде `router`** — ✅ уже покрыто, сверено 2026-09-16: правило через VPS кладёт адрес в `smart_vps` и уводит устройство через VPS, до запроса имени устройство идёт напрямую; имя, спрошенное сразу после сайта, выучивается за ним, `vibedpn rule forget` снимает. Проходит в CI (задание «box + fake VPS + LAN device»)
  - [x] **CDN по CNAME и правило `direct` сильнее списка на стенде `router`** — ✅ (2026-09-16, `next`) прошло в CI (прогон 35084161211): `rule direct: 10-163-52-12.nip.io in smart_direct although its list goes through the VPS`, `CNAME: github.com follows www.github.com through smart_vps`. CNAME: правило `www.github.com` через VPS без автообучения, отдельный запрос цели `github.com` уходит каналом `smart_vps`. `direct`: список через VPS и правило `direct` на то же имя — канал `smart_direct`, адрес в `smart_direct` и не в `smart_vps`
  - [x] **две страны на стенде `home`** — ✅ (2026-09-14, `next`) правила DE и NL в `smart`, `vibedpn restart`: контейнеры `myst-consumer-de`/`-nl` запущены, `ip rule` 0x40→7740 и 0x41→7741 с `unreachable default`, ядро создало identity в каждой стране (в `/status` `dpn_countries` — две identity и адреса пополнения), TequilAPI страны из LAN недоступен; стенд прошёл целиком. Реальное подключение в стране не проверено — нужны регистрация и MYST
- [x] **Импорт готовых списков (по URL, с кэшем)** — ✅ (2026-09-14, `next`) `routing.lists`: URL и канал всех доменов (`via`, `country`), решение 21; у правила и списка общий канал — наборы, метки, аплинки и consumer стран строятся по обоим. `engine/domainlists.py`: разбор домена, hosts-строки и `||домен^` (пути, маски и исключения пропускаются), копия на URL в `data/core/lists/`, скачивание без прокси окружения, только 200 и не больше 16 МиБ, страница без доменов копию не заменяет. Цикл ядра раз в 10 с: новый список — сразу, свежий — раз в сутки, недоступный — раз в 15 минут по копии; домены в индексе резолвера под правилами, их держит резолвер, поэтому перезапуск цикла снимает ушедшие. `GET/PUT/DELETE /lists`, `vibedpn lists show|add|rm`, раздел «Domain lists» на странице «Rules». Тесты разбора, кэша, скачивания, цикла, API и CLI; стенд `router.sh`: список с локального HTTP-сервера через `vibedpn lists add`, ядро скачало 1 домен, имя из списка прошло через `smart_vps`, стенд прошёл целиком; в браузере не проверено

## Stage 11 — Полировка и раздача

- [x] **DNS-запросы AdGuard идут через `default_upstream` в режиме `full`** — ✅ (2026-09-14, `next`) AdGuard работает под UID 7753 (`user` и `NET_BIND_SERVICE` в `compose.yaml`, ядро при старте отдаёт ему `data/adguard/{conf,work}`); в `full` роутер добавляет цепочку `dns_uplink` (`type route hook output`): трафик этого UID, кроме LAN и адресов коробки, получает метку активного аплинка, IPv6 мимо `lo` отбрасывается. Исполнением на VM: AdGuard не от root поднимает порт 53, метка `skuid` уводит DoH процесса через шлюз, `nft -c` принимает набор. `doctor --network`: `dns leak` — OK при загруженной цепочке. Стенд `router.sh`: без шлюза при `failopen: false` AdGuard не разрешает новое имя, со шлюзом — разрешает. Решение 14. Попутно в стенде исправлены устаревшие ожидания: второе устройство ждётся раунд обнаружения, `mode off/full` проверяются как применённые на лету без перезапусков
- [x] **`vibedpn backup|restore`** — ✅ (2026-09-13, `next`) `engine/backup.py`: архив `tar.gz` (600) из `config.yaml`, `.env`, `secrets/`, `data/`; `backup` останавливает работающие сервисы на время копии и поднимает их обратно; `restore` проверяет все члены архива до записи (абсолютные пути, `..`, ссылки наружу, устройства — отказ целиком), откладывает текущие файлы в `restore-backup-<время>` и распаковывает с сохранением владельцев (фильтр `tar` там, где он есть: в bookworm Python 3.11.2 его нет). Решение 12. Unit-тесты и стенд `gateway.sh`: копия работающей коробки, коробка поднялась, после `restore` секрет вернулся, заменённый лежит в стороне, после `up` устройство на месте
- [ ] `vibedpn update` и автообновление по таймеру (opt-in) — код и unit-тесты есть (`install.sh` из клона на текущей ветке, `compose pull --ignore-buildable`, `restart` новым CLI; `update --timer on|off` — `vibedpn-update.timer`, воскресенье 04:00, `RandomizedDelaySec=1h`, `Persistent=true`), исполнением не проверено: `install.sh` работает только на Debian, стенд на Ubuntu-VM его не запустит — проверка на N100
- [ ] `docs/troubleshooting.md` по реальным вопросам из чата
- [ ] Релиз v0.1.0: теги образов, GitHub Release, одна строка установки в README

## Stage 12 — Образ для Pi (опционально)

- [ ] `pi-gen` пайплайн: готовый `.img` с предустановленным VibeDPN, first-boot запускает `init` — реализовано, ни один образ не собран (2026-09-14, `next`): решения 13 и 15 (amd64 и arm64, без armhf; ни одного секрета, доступ через cloud-init, `init` при первом входе); `images/os/common` (`provision.sh` через `install.sh` в режиме `VIBEDPN_IMAGE_BUILD=1`, скрипт первого входа, NoCloud с меткой `CIDATA`, образец `user-data`), `images/os/debos/vibedpn.yaml` (UEFI amd64/arm64, GRUB EFI), `images/os/pi-gen` (стадия на `pi-gen` `arm64@86919dae`, сверена с его исходниками), workflow `os-images` (вручную и по тегу), мануал `osImages.md`. Проверка: на colima VM debos в Docker без KVM падает после `debootstrap` (`systemd-nspawn` в контейнере) — сборка и загрузка образов ждут прогона workflow на раннерах и N100/Raspberry Pi

## Предложения (не выполнять без команды оператора)

- [ ] Подбор CDN к сайту с помощью ИИ поверх журнала DNS (возможна платная функция) — отдельным шагом после Stage 10

- Второй DPN-бэкенд: Sentinel dVPN как ещё один gateway-контейнер
- Telegram-бот: алерты «туннель лёг», еженедельный отчёт по заработку ноды
- Несколько VPS-пиров с failover
- Интеграция с fresta: коробка как источник whitelist-конфигов для мобильных
- Web-обновлялка (кнопка «обновить» в UI)
